# Fruit Drop Agent Handoff & Session State

**Project**: Fruit Drop (`fruit-drop`) — Zero-Friction Pre-order & Car Park Tailgate Dispatch Engine  
**Version**: `0.10.4`  
**Date**: 2026-09-08  
**Remote Git**: `git@github.com:kwancanoe/fruit-drop.git`  
**Firebase Project**: `fruit-drop-aon` (Region: `asia-southeast1`)  
**Production URL**: `https://fruitdrop.web.app` (Secondary/Default Alias: `https://fruit-drop-aon.web.app`)  

---

## 1. What Was Completed in This Session (v0.10.4 - Multi-Machine Synchronization & Reconciliation)
1. **Upstream Git Synchronization & Merge**:
   - Integrated commits from work machine (`origin/main` v0.9.10 - v0.9.12) with local machine commits (v0.10.0 - v0.10.3) without data loss or regressions.
   - Preserved workplace enhancements:
     - Dependency: Installed `register-service-worker` in `package.json` and `pnpm-lock.yaml`.
     - RBAC: Confined `SELLER` role to frontline dispatch only (`AdminLayout.vue`, `routes.ts`, `router/index.ts`, `OrderDetailPage.vue`, and `firestore.rules`).
     - Auth: Whitelisted `kwancanoe@gmail.com` profile bootstrap and switched to `setDoc(..., { merge: true })` in `userStore.ts`.
     - Tests: Added 12 new tests in `router_guard.test.mjs` and `security_rules.test.mjs`.
   - Preserved home enhancements:
     - Core: Migrated round dates to Firestore Timestamp with automatic 24:00 cutoff (`roundDate.ts`, `fruitStore.ts`).
     - UI: Minimal mode on `QDate`, highlighted selected date indicator, and removed redundant standby time banner.
     - Infrastructure: Configured dual-target multisite deploy in `firebase.json` (`fruitdrop` and `fruit-drop-aon`).
     - SEO: Polish social preview metadata and OG tags ("ผลไม้สด ส่งตรงจากสวน").
2. **Conflict Resolution & Verification**:
   - Resolved merge conflicts in `package.json` and `.agent_handoff.md`.
   - All 3-way code merges in `OrderDetailPage.vue` verified and validated.
   - Bumped version to `0.10.4`.

---

## 2. What Was Completed in Previous Session (v0.10.3 - Social Preview Copywriting Polish: "ผลไม้สด ส่งตรงจากสวน")
1. **Refined Social Sharing Copy (`index.html`)**:
   - Replaced robotic AI description `"Fruit Drop - สั่งจองผลไม้สด นัดรับท้ายรถ"` with authentic Thai orchard branding: `"Fruit Drop - ผลไม้สด ส่งตรงจากสวน"`.
   - Updated both `<meta name="description">` and `<meta property="og:description">`.
   - Added standard Twitter Card metadata (`twitter:card`, `twitter:title`, `twitter:description`, `twitter:image`) for universal social link previews (LINE, Facebook, X, Messages).
2. **Verification & Pipeline Integrity**:
   - `pnpm run typecheck`: **0 errors**.
   - Unit tests: **144 tests passed (0 failures)**.
   - `pnpm run build`: **Build succeeded (0 errors)**.
   - Bumped version to `0.10.3` in `package.json` and `.agent_handoff.md`.

---

## 2. What Was Completed in Previous Session (v0.10.2 - Dual-Target Multisite Deploy: fruitdrop.web.app Activation)
1. **Root Cause Analysis (Why fruitdrop.web.app was not working)**:
   - When the user created the secondary site `fruitdrop` in the Firebase Console and linked it to the web app, Firebase CLI locally still had no deployment targets configured.
   - `firebase.json` had a single default `"hosting"` block without a `"site"` or `"target"`, so all CLI deploys exclusively went to the project ID site (`fruit-drop-aon`), leaving `fruitdrop` in a `"Waiting for your first release"` state.
2. **Dual-Target Multisite Configuration (`.firebaserc` & `firebase.json`)**:
   - Registered CLI deploy targets:
     - `firebase target:apply hosting fruitdrop fruitdrop`
     - `firebase target:apply hosting fruit-drop-aon fruit-drop-aon`
   - Configured `firebase.json` with an array of two hosting targets (`fruitdrop` and `fruit-drop-aon`), ensuring every build/deploy publishes to both URLs simultaneously (zero-downtime, zero-regression).
3. **Live Deployment & Verification**:
   - Deployed 95 files to both hosting targets.
   - Verified live HTTP response from `https://fruitdrop.web.app` (HTTP 200, Title: `"Fruit Drop - สั่งจองผลไม้สด"`).
4. **Bumped Version & Committed**:
   - Bumped version to `0.10.2` in `package.json` and `.agent_handoff.md`.

---

## 2. What Was Completed in Previous Session (v0.10.1 - Calendar Minimal Mode, Selection Indicator & Redundant Banner Removal)
1. **Removed Bulky Green Top Header in Calendar (`src/pages/admin/RoundEditPage.vue`)**:
   - Added the `minimal` prop to `<q-date>`. This completely removes the legacy Material Design 2 header block (which previously showed empty underscores `_ _` due to uninitialized models) and renders a clean, compact Material Design 3 month grid starting directly with month/year navigation.
2. **Fixed Selected Date Indicator on `<q-date>`**:
   - Root cause: `<q-date>` was configured with `mask="YYYY/MM/DD"` (slashes) while `calendarDate` and `onCalendarDatePicked` stored dates in ISO format `"YYYY-MM-DD"` (hyphens), and in new round mode `calendarDate` was initialized to `''`. The string mismatch caused Quasar to fail to identify the selected date, suppressing the active circular green highlight.
   - Fix: Changed `mask="YYYY-MM-DD"`, standardized `calendarDate` on pure ISO strings, initialized `calendarDate` to today's date in creation mode, and unified proxy invocation directly on the `<q-input>` wrapper while eliminating duplicate `<q-date>` in append slot. The active date is now cleanly highlighted with the solid green circle indicator.
3. **Eliminated Redundant Standby Time Banner**:
   - Removed the redundant green preview card (`เวลารับของ: 19:00 - 23:00 น.`) located below the start and end time inputs in Section 1. Preserved `computedStandbyTime` in script for payload synchronization.
4. **Verification & Pipeline Integrity**:
   - `pnpm run typecheck` (`vue-tsc --noEmit`): **0 errors**.
   - Full unit test suites: **144 tests passed across 4 test suites (0 failures)**.
   - `pnpm run build` (`quasar build -m pwa`): **Build succeeded (0 errors)**.
   - Bumped version to `0.10.1` in `package.json` and `.agent_handoff.md`.

---

## 2. What Was Completed in Previous Session (v0.10.0 - Firestore Timestamp Migration & Automatic 24:00 Round Cutoff)
1. **Standardized Date & Time Architecture (`src/utils/roundDate.ts`)**:
   - Built a comprehensive, polymorphic date engine handling Firestore `Timestamp`, serialized `{ seconds, nanoseconds }`, JavaScript `Date`, numeric epoch milliseconds/seconds, ISO strings (`YYYY-MM-DD`), and legacy Thai strings (`"วันอังคารที่ 8 กันยายน 2569"`).
   - Standardized display formatting via `formatThaiPickupDate(val, 'full' | 'short')` to produce authentic Thai dates (`"วันอังคารที่ 8 กันยายน 2569"` or `"8 ก.ย. 2569"`) without storing raw strings in the database.
   - Implemented `createRoundTimestamp(val)` to ensure all round dates are persisted as midnight (`00:00:00`) Firestore `Timestamp` objects.
2. **Automatic 24:00 Round Cutoff & Booking Closure**:
   - `getRoundCutoffTimestamp(round)` calculates the exact local end-of-day boundary (`23:59:59.999`) for the round date.
   - `isRoundPastCutoff(round)` verifies whether current time has passed the 24:00 threshold.
   - **Storefront**: `subscribeToOpenRounds()` in `fruitStore.ts` strictly filters `openRounds.value = fetched.filter(r => r.isOpen && !isRoundPastCutoff(r));`. Past 24:00, expired rounds automatically disappear from customer view without requiring manual admin intervention.
   - **Store Level Guard**: `submitOrder()` validates round freshness and rejects late pre-orders if `isRoundPastCutoff(round)` is true.
   - **Background Synchronization**: `subscribeToAllRounds()` automatically triggers `toggleRoundStatus(r.roundId, false)` in the background for any open round that has passed 24:00.
   - **Admin Management & Dispatch**:
     - `RoundsPage.vue`: Displays a `สิ้นสุดรอบ (ผ่าน 24:00 น.)` grey badge and disables the toggle switch for expired rounds.
     - `DispatchPage.vue`: Retains expired rounds in the selector (labeled `[หมดรอบ]`) so tailgate staff can complete scale weighing, cash collection, and order handover without disruption.
3. **Database Migration on Live Firestore**:
   - Migrated `ROUND-001` and `ROUND-604612` documents in Firestore to store standard `pickupDate` (Timestamp), `pickupDateIso` (`'2026-09-08'` / `'2026-09-09'`), `pickupDateTimestamp` (epoch ms), and normalized military time slots `pickupSlots` (`['19:00', '19:30', ...]`).
4. **Time Slot Normalization & COOP Header Fix**:
   - Unified slot display across `TailgateOrderCard.vue`, `AnalyticsPage.vue`, `OrderDetailPage.vue` (customer and admin), `OrderQueueCard.vue`, and `orderTicketCanvas.ts` using `normalizeSlotLabel()`.
   - Added `"Cross-Origin-Opener-Policy": "same-origin-allow-popups"` in `firebase.json` to resolve Chrome window-closed pop-up console warning during authentication flows.
5. **Verification & Pipeline Integrity**:
   - Full automated test suite: **144 unit tests passing across 4 test suites (0 failures)**.
   - `pnpm run typecheck` (`vue-tsc --noEmit`): **0 errors**.
   - `pnpm run build` (`quasar build -m pwa`): **Build succeeded (0 errors)**.
   - Bumped version to `0.10.0` in `package.json` and `.agent_handoff.md`.

---

## 2. What Was Completed in Previous Session (v0.9.12 - Deep Code Audit & Whitelisted Admin Bootstrap Fix)
1. **Forensic Audit of RBAC & Security Boundaries**:
   - Performed deep source-level code audit on all 9 modified files across client routes, layout navigation, order actions, and Firestore security rules.
   - Verified zero regression on SELLER least-privilege confinement: SELLER cannot see rounds, P&L, or staff management; cannot create/edit/delete rounds; cannot cancel/revert orders; cannot write to rounds/products/master_fruits or delete orders in Firestore.
2. **Fixed System Admin Bootstrap Fallback (`src/stores/userStore.ts`)**:
   - Identified that `kwancanoe@gmail.com` was missing from the local fallback mapping in `bindAuthUser()` despite being in `ADMIN_WHITELIST_EMAILS` and Firestore rules.
   - Added bootstrap profile for `kwancanoe@gmail.com` (`role: 'SYSTEM_ADMIN'`).
   - Replaced fragile `updateDoc` with `setDoc(userDocRef, ..., { merge: true })` so newly authenticated whitelisted admins automatically provision their Firestore profile with telemetry on first login.
3. **Comprehensive Verification & Production Deployment**:
   - `pnpm run typecheck` (`vue-tsc --noEmit`): **0 errors**.
   - `node --test tests/unit/*.test.mjs`: **284 passed, 0 failed**.
   - `pnpm run build` (`quasar build -m pwa`): **Build succeeded (0 errors)**.
   - Deployed to Firebase (`fruit-drop-aon`): **Hosting, Firestore Rules, Storage Rules** successfully released at `2026-09-08 16:06:15`.
   - Bumped version to `0.9.12` in `package.json` and `.agent_handoff.md`.

---

## 2. What Was Completed in Previous Session (v0.9.11 - SELLER Role Hardening & Least-Privilege Confinement)
1. **Frontend Navigation Lockdown (`src/layouts/AdminLayout.vue`)**:
   - Hidden the "รอบส่ง" (`/admin/rounds`) tab from the bottom navigation bar for `SELLER` accounts using `v-if="userStore.isSystemAdmin || userStore.isShopOwner"`. Sellers now see only the tailgate desk ("ท้ายรถ").
2. **Router Guards Lockdown (`src/router/routes.ts` & `src/router/index.ts`)**:
   - Added `meta: { disallowedRoles: ['SELLER'] }` to `/admin/rounds`, `/admin/rounds/new`, and `/admin/rounds/:roundId/edit`.
   - Included `'/admin/rounds'` in the router guard prefix check, immediately redirecting any unauthorized direct URL entry to `/admin` with a warning notification ("คุณไม่มีสิทธิ์เข้าถึงหน้านี้").
3. **Order Detail Action Lockdown (`src/pages/admin/OrderDetailPage.vue`)**:
   - Restricted administrative and reversal operations to `userStore.isSystemAdmin || userStore.isShopOwner`:
     - Cancel order button (`btn-open-cancel-dialog`)
     - Revert delivery button (`handleRevertDelivery`)
     - Revert cancellation button (`handleRevertCancellation`)
   - Sellers are strictly confined to frontline dispatch actions: weighing scale inputs, customer calls, photo proof attachment, and cash/transfer delivery confirmation.
4. **Database Security Hardening (`firestore.rules`)**:
   - Pre-validated with `firebase_validate_security_rules` -> OK.
   - Restricted write access on `/rounds/{roundId}`, `/products/{productId}`, and `/master_fruits/{fruitId}` to `isSystemAdmin() || isShopOwner()`.
   - Separated `/orders/{orderId}` write permissions: `allow update: if isStaff()` (allowing sellers to fulfill orders) while `allow delete: if isSystemAdmin() || isShopOwner()`.
5. **Comprehensive Automated Verification**:
   - Unit tests: Added 12 new tests in `router_guard.test.mjs` and `security_rules.test.mjs` covering SELLER blocking. Total: **284 passed, 0 failed** across 62 suites.
   - Typecheck (`pnpm run typecheck`): **0 errors**.
   - Build (`pnpm run build`): **Succeeded (0 errors)**.
   - Headless CDP Browser E2E (`node scripts/test_e2e_cdp.mjs`): **All steps passed with Exit Code 0**.
   - Bumped version to `0.9.11` in `package.json` and `.agent_handoff.md`.

---

## 2. What Was Completed in Previous Session (v0.9.10 - Remote Sync & PWA Dependency Fix)
1. **Upstream Git Synchronization**:
   - Pulled 6 commits from `origin/main` (`77b2469`), synchronizing all recent work from home machine (Milestones 1-4, headless CDP browser test suite, and dispatch UI polish).
2. **Resolved Missing PWA Dependency (`register-service-worker`)**:
   - Detected missing `register-service-worker` package causing `vue-tsc --noEmit` error TS2307 on clean machine.
   - Installed `register-service-worker` via `pnpm add register-service-worker` and verified lockfile integrity.
3. **Verification & Pipeline Integrity**:
   - `pnpm run typecheck` (`vue-tsc --noEmit`): **0 errors**.
   - `node --test tests/unit/*.test.mjs`: **272 passed, 0 failed**.
   - `pnpm run build` (`quasar build -m pwa`): **Build succeeded (0 errors)**.
   - Bumped version to `0.9.10` in `package.json` and `.agent_handoff.md`.

---

## 2. What Was Completed in Previous Session (v0.9.9 - UI Polish & Storefront Lifecycle Clarification)
1. **Dispatch Page UI Streamlining (`src/pages/admin/DispatchPage.vue`)**:
   - Removed redundant `btn-manage-rounds-shortcut` button (`icon="tune"`) next to the round selector. (The bottom navigation bar already permanently provides the "รอบส่ง" tab `/admin/rounds`).
   - Unwrapped the unnecessary horizontal row container so `q-select#select-active-round` takes full 100% card width. This eliminates title text truncation (`...`) on mobile devices.
2. **Storefront Date Expiry & Lifecycle Audit**:
   - Verified that `pickupDate` in Firestore is a human-readable display string (`"วันอังคารที่ 8 กันยายน 2569"`), not an automatic TTL filter.
   - Verified that customer storefront round availability is 100% controlled in real-time by `where('isOpen', '==', true)` in `subscribeToOpenRounds()`.
   - Confirmed storefront behavior when round dates pass: if open, customers can still view and book; once admin toggles `isOpen = false`, the round disappears immediately via `onSnapshot` and renders the empty state card (`"ยังไม่มีรอบเปิดจอง"`) with customer order lookup active.
3. **Verification & Pipeline Integrity**:
   - `pnpm run typecheck` (`vue-tsc --noEmit`): **0 errors**.
   - Unit tests (`tests/unit/pricing_and_state.test.mjs`): **93 passed, 0 failed**.
   - `pnpm run build` (`quasar build -m pwa`): **Build succeeded (0 errors)**.
   - Bumped version to `0.9.9` in `package.json` and `.agent_handoff.md`.
1. **Feature 17: Programmatic Verification Suite (`tests/unit/pricing_and_state.test.mjs`)**:
   - Built full 4-tier testing methodology using native `node:test` and `node:assert/strict` with 93 tests across 24 suites (100% pass, 0 fail):
     - **Feature 1 (Fixed-Weight Bundle Pricing)**: 5 Tier 1 happy-path cases (1kg, 3kg bundle, 5kg bundle, multiple bundles, exact label match) + 5 Tier 2 boundary cases (0kg zero-guard, -1kg/-5kg negative weights, 2.5kg fractional, unbundled 4kg, 6kg exceeding bundle).
     - **Feature 2 (Durian Scale & Tare Math)**: 5 Tier 1 happy-path cases (net weight deduction, single durian, multiple durians, pricePerKg scaling, zero-tare) + 5 Tier 2 boundary cases (0kg unweighed, negative gross, tare exceeding gross, fractional grams precision, unweighed gating).
     - **Feature 3 (Order Total & Item Subtotals)**: 5 Tier 1 happy-path cases (single, mixed bundle/durian, bundle vs non-bundle, final price override, multi-durian) + 5 Tier 2 boundary cases (empty cart, all 0kg, 500kg bulk, float rounding, invalid item handling).
     - **Feature 4 (Order State Transitions)**: 5 Tier 1 valid transitions (`WAITING_PICKUP` -> `COMPLETED` cash/transfer, `CANCELLED`, revert, `WEIGHING` resolution) + 5 Tier 2 boundary cases (reject `COMPLETED` -> `CANCELLED`, reject `COMPLETED` -> `WAITING_PICKUP`, invalid status strings, reason preservation, timestamp cleanup).
     - **Feature 5 (Handover Payment Attribution)**: 5 Tier 1 attribution checks (CASH attribution, TRANSFER attribution, staff identity, timestamp, device fingerprint) + 5 Tier 2 boundary cases (disjoint sets, legacy cash fallback, legacy transfer fallback, CANCELLED exclusion, WAITING exclusion).
     - **Feature 6 (Cross-Round Order Lookup)**: 5 Tier 1 search normalization cases (exact `FD-1082`, `#FD-1082`, `1082`, formatted phone `081-234-5678`, unformatted phone) + 5 Tier 2 boundary cases (empty string, whitespace, short digits < 4, `№` symbol, case-insensitivity).
     - **Feature 7 (Time Slot Generation & Overnight)**: 5 Tier 1 daytime windows (30m, 15m, 60m, format normalize, single slot) + 5 Tier 2 boundary cases (overnight 22:00->02:00 modulo 1440 wrap, 23:30->00:30, 00:00 midnight, invalid string fallback, non-positive step).
     - **Tier 3 (Pairwise Combinations)**: 18 combinations of Payment Modes (`CASH`, `TRANSFER`) x Fruit Types (`FIXED_WEIGHT`, `VARIABLE_WHOLE_FRUIT`, `MIXED`) x Order Statuses (`WAITING_PICKUP`, `COMPLETED`, `CANCELLED`).
     - **Tier 4 (Real-World Application Scenarios)**: 5 end-to-end user journeys (Pre-order to Pickup, Tailgate Scale Weighing & PromptPay Delivery, Tailgate Cash Collection & Attribution, Order Cancellation & Revert Flow, Cross-Round Customer Search).
2. **Feature 18: Automated Headless Browser Testing via CDP (`scripts/test_e2e_cdp.mjs`)**:
   - Zero-dependency standalone automation runner using Node built-in `http`, `child_process.spawn`, and native `WebSocket` / `fetch`.
   - Spawns headless Chrome/Edge (`--headless=new`, `--remote-debugging-port`, `--disable-gpu`).
   - Serves `dist/pwa` over local HTTP server with SPA fallback and accurate MIME types.
   - Connects to CDP WebSocket endpoint, enables `Page`, `Runtime`, `Console`, `DOM`.
   - Verifies Storefront: title `"Fruit Drop - สั่งจองผลไม้สด"`, Open Graph meta tags, zero default Quasar/boilerplate text.
   - Verifies Customer Order Lookup modal: button click, dialog `#order-lookup-modal` rendered in DOM, clean dismissal.
   - Verifies Admin Desk Gate: navigates to `/#/admin`, verifies `ระบบแอดมิน Fruit Drop` login container.
   - Verifies Invariants: 0 runtime exceptions, 0 critical console errors; graceful browser termination and cleanup (Exit Code 0).
3. **Feature 19: Build, Publish & Final Gate Verification**:
   - Published comprehensive test readiness document `TEST_READY.md`.
   - Extracted DRY utilities: `src/utils/orderSearch.ts` (search candidates & in-memory matching), `src/utils/pricing.ts` (`calculateNetWeight`, `calculateWeighedFruitPrice`, `calculateCashInHandTotal`, `calculatePrepaidTransferTotal`), `src/constants/status.ts` (`isValidOrderTransition`).
   - Full test suite: 272 unit tests across 9 test files passed (100%).
   - `pnpm run typecheck` (`vue-tsc --noEmit`): 0 errors.
   - `pnpm run build` (`quasar build -m pwa`): Build succeeded (0 errors).
   - Bumped version to `0.9.8` in `package.json` and `.agent_handoff.md`.

---

## 2. What Was Completed in Previous Session (v0.9.7 - Milestone 3: Performance, Lifecycle Cleanup, Image Compression, DRY & Quasar/M3 Compliance)
1. **Feature 12: Firestore Listener Lifecycle & Cleanup**:
   - `fruitStore.ts`:
     - Renamed internal listener variables to `openRoundsUnsub`, `allRoundsUnsub`, `productsUnsub`, `masterFruitsUnsub`, `ordersUnsub`.
     - Provided and exported individual unsubscription functions: `unsubscribeOpenRounds()`, `unsubscribeAllRounds()`, `unsubscribeProducts()`, `unsubscribeMasterFruits()`, `unsubscribeOrders()`.
     - Exported comprehensive cleanup functions: `unsubscribeAll()` and `cleanupStore()`.
     - In `logoutAdmin()`: invoked `cleanupStore()` and `userStore.cleanupStore()` before `signOut(auth)`.
     - In subscription methods: invoked corresponding unsubscription handles before attaching new listeners to prevent stacking.
   - `userStore.ts`:
     - Added and exported `unsubscribeUsers()` and `cleanupStore()` (clearing `users.value = []`, `currentAppUser.value = null`).
   - `AdminLayout.vue`:
     - Replaced unconditional mounted subscriptions with reactive authentication watching (`watch([() => fruitStore.authUser, () => fruitStore.isAdmin])`); tears down listeners via `stopAdminSubscriptions()` on logout or when unauthenticated.
     - Added `onBeforeUnmount(() => stopAdminSubscriptions())`.
   - `FruitsPage.vue` & `RoundEditPage.vue`:
     - Added `onBeforeUnmount(() => fruitStore.unsubscribeMasterFruits())`.
2. **Feature 13: Storefront Read Leak Prevention**:
   - `MainLayout.vue`:
     - Removed `fruitStore.subscribeToOrders(fruitStore.activeRoundId)` from `onMounted()`, preventing customer clients from leaking and reading all round orders.
     - Removed `:orders="fruitStore.orders"` binding to `<OrderLookupModal>`.
     - Added `onBeforeUnmount(() => fruitStore.unsubscribeOpenRounds())`.
3. **Feature 14: Tailgate Photo Compression Pipeline**:
   - `imageCompressor.ts`:
     - Enhanced memory safety with `URL.createObjectURL(file)` + prompt `URL.revokeObjectURL()`, robust bounding box aspect ratio scaling with `Math.min(maxWidth / width, maxHeight / height)`, and canvas memory zeroing (`canvas.width = 0; canvas.height = 0`).
   - `fruitStore.ts`:
     - Added store-level automatic compression guard in `uploadPaymentProof` if `imageBlob.size > 300KB` or if a raw `File` is passed, before sending to Firebase Storage.
   - `OrderDetailPage.vue`:
     - In `handleFileSelected`: compressed photo prior to upload (1200x1200px JPEG quality 0.75), rendered local preview with compressed data URL, revoked old preview URLs, notified Quasar positive toast with compressed vs original size in KB, and reset `target.value = ''`.
     - Added `onBeforeUnmount` cleanup for `localPreviewUrl`.
4. **Feature 15: DRY Deduplication**:
   - `TailgateOrderCard.vue` & `OrderDetailPage.vue`:
     - Replaced duplicate inline durian weighing checks with centralized `orderHasUnweighedFruit(order)` imported from `@/constants/status`.
   - `AnalyticsPage.vue`:
     - Replaced inline ternary status badge with `<OrderStatusBadge :order="order" dense />`.
   - `RoundEditPage.vue`:
     - Replaced raw emoji status string with `<RoundStatusBadge :is-open="form.isOpen" />`.
   - `UsersPage.vue`:
     - Deduplicated `canEdit` and `canDelete` into unified `canManageTargetUser(targetUser: AppUser)`.
5. **Feature 16: Strict Quasar Purity & Design Tokens**:
   - `QrScannerPage.vue`:
     - Moved inline styles from viewfinder and corners to `<style scoped lang="scss">` using `$positive` and Quasar classes.
   - `FruitEditDialog.vue`:
     - Replaced `#2E7D32` with `$positive`.
   - `FruitChip.vue`:
     - Replaced inline hex gradient and text colors with `$gradient-tonal-positive` and `$color-positive-dark`.
6. **Verification & Pipeline Integrity**:
   - Unit test suite (`tests/unit/*.test.mjs` including new `milestone3_lifecycle_dry_compression.test.mjs`): **126 passed, 0 failed**.
   - `pnpm run typecheck` (`vue-tsc --noEmit`): **0 errors**.
   - `pnpm run build` (`quasar build -m pwa`): **Build succeeded (0 errors)**.

---

## 2. What Was Completed in Previous Session (v0.9.6 - Milestone 2: Mock Purge, Data Contracts & Mathematical Precision)
1. **Feature 6: Mock & Stub Elimination**:
   - `fruitStore.ts`: Safely removed `getDefaultProducts` (141 lines of hardcoded mock items) and mock fallback in `subscribeToProducts` (setting `products.value = []` when snapshot is empty).
   - `createRound` & `updateRound`: Removed hardcoded fallback arrays for `ngo`, `mangkut`, `longkong`, `thurian`; dynamically pull bundles and size tiers directly from `masterFruits` / `DEFAULT_MASTER_FRUITS`.
   - `subscribeToMasterFruits`: Removed client-side auto-seeding `setDoc` write loop inside `onSnapshot` listener to prevent unauthorized write permission denial and listener side-effects.
   - `seedMasterData`: Dynamically seeds `ROUND-001` products from `DEFAULT_MASTER_FRUITS`.
2. **Feature 7: Dead Code Purge**:
   - Safely deleted unused template store `src/stores/example-store.ts` (`useCounterStore`).
3. **Feature 8: Pricing Engine Contract Refactoring**:
   - `pricing.ts`: Replaced fragile regex parsing (`/(\d+)\s*บาท/`) of `orderedBundle` with typed contracts (`ProductBundle.qtyKg === item.orderedKg` and direct `item.bundlePrice` snapshot).
   - Fixed zero-quantity fallback bug where `0 || 1` evaluated to `1` in `(Number(item.orderedKg) || 1) * item.pricePerKg`; added explicit `orderedKg <= 0 ? 0 : ...` zero guard so 0 kg orders accurately calculate as 0 THB.
4. **Feature 9: COGS Snapshot Isolation**:
   - `fruit_app.ts`: Extended `OrderItem` interface with `costPerKg?: number`, `bundleQtyKg?: number`, `bundlePrice?: number`.
   - `FruitSelector.vue` & `fruitStore.ts`: Snapshot `costPerKg` from `ProductItem` into `OrderItem` at checkout and defensively sanitize payload via `sanitizeFirestoreData()`.
   - `AnalyticsPage.vue`: Eliminated 11 hardcoded fallback cost IDs (`PROD-NGO: 20`, etc.) and Thai name heuristics (`includes('เงาะ')`); replaced with immutable `item.costPerKg` snapshot lookup with dynamic fallback to catalog. Fixed zero-kg bug in `getItemWeightKg`.
5. **Feature 10: Overnight Time Slot Support**:
   - `timeSlots.ts`: Enhanced `generateTimeSlots` to support overnight windows spanning midnight (`startMin > endMin`) via modulo 1440 wrapping math; eliminated hardcoded 5-slot dummy fallback array `['19:00 น.', ...]`.
   - `DispatchPage.vue`: Sorted dispatch tabs chronologically relative to standby window `startMin`.
6. **Feature 11: Comprehensive Error Notifications**:
   - `customer/OrderDetailPage.vue`: Isolated network/Firestore errors from 404 in `loadOrder()`; rendered M3 16px error card (`id="card-order-load-error"`) with retry button (`btn-retry-load-order`) and back button.
   - `admin/OrderDetailPage.vue`: Added Quasar negative notification in `doRevert()`.
   - `IndexPage.vue`: Wrapped `checkActiveCustomerOrder()` in `try ... catch`, filtered strictly for `WAITING_PICKUP` orders, and removed dummy fallback in `availablePickupSlots`.
7. **Verification & Pipeline Integrity**:
   - Unit test suite (`tests/unit/pricing_cogs_slots.test.mjs` + existing suites): **68 passed, 0 failed**.
   - `pnpm run typecheck` (`vue-tsc --noEmit`): **0 errors**.
   - `pnpm run build` (`quasar build -m pwa`): **Build succeeded (0 errors)**.

---

## 2. What Was Completed in Previous Session (v0.9.5 - Milestone 1)
1. **Feature 1: Handover Attribution Fix & Mutually Disjoint Financials**:
   - `OrderDetailPage.vue`: Explicitly set `paymentMethod: 'PROMPTPAY_PREPAID'` and synchronized `attribution` in `handleConfirmTransferAndDeliver`.
   - `fruitStore.ts`: Corrected handover attribution ternary priority to `updates.paymentMethod ?? targetOrder.paymentMethod` and added doc ID resolution fallback.
   - `DispatchPage.vue`: Formulated `cashInHandTotal` and `prepaidTotal` into mutually disjoint sets based on `order.attribution?.paymentModeAtHandover` and excluded cancelled orders.
   - `status.ts`: Added `paymentMethod` fallback in `getOrderStatusConfig`.
2. **Feature 2: Order Cancellation State Machine**:
   - `fruit_app.ts`: Added `cancelledAt?: number` and `cancelReason?: string` to `Order` interface.
   - `fruitStore.ts`: Implemented `cancelOrder(orderId, reason)` and `revertOrderCancellation(orderId)` with Firestore synchronization.
   - `OrderDetailPage.vue`: Added cancellation confirmation dialog with reason presets, danger cancellation button, and red M3 cancellation banner with revert action.
3. **Feature 3: Security Rules & Storage Hardening**:
   - `firestore.rules`: Restricted self-update on `users/{userEmail}` to `request.resource.data.diff(resource.data).affectedKeys().hasOnly(['lastLoginAt', 'lastLoginDevice', 'uid', 'updatedAt'])`. Pre-validated with `firebase_validate_security_rules` -> OK.
   - `storage.rules`: Enforced `< 5 * 1024 * 1024` and `contentType.matches('image/.*')` on all payment proof and fruit uploads. Pre-validated with `firebase_validate_security_rules` -> OK.
4. **Feature 4: Admin Route Navigation Guards & RBAC**:
   - `routes.ts`: Added `meta: { requiresAuth: true }` to `/admin` and `meta: { disallowedRoles: ['SELLER'] }` to `/admin/analytics`, `/admin/fruits`, `/admin/users`.
   - `router/index.ts`: Installed global `Router.beforeEach` guard with `auth.authStateReady()`, auto user store resolution, unauthenticated redirect to `/admin`, and seller restriction enforcement with Quasar notification.
   - `AdminLayout.vue`: Added `v-if="userStore.isSystemAdmin || userStore.isShopOwner"` to the analytics tab.
5. **Feature 5: Cross-Round Search Robustness & Customer Modal M3**:
   - `fruitStore.ts`: Implemented `extractSearchCandidates` (stripping `#№`, extracting 4-digit orderId suffixes, normalizing Thai/international phone formats) and refactored `searchOrderAcrossRounds` and `getOrderByOrderId` to use candidate arrays without arbitrary 50-limit fallback.
   - `OrderLookupModal.vue`: Upgraded to M3 16px surface card and pill button styling with informative search instructions.
6. **Verification & Pipeline Integrity**:
   - `pnpm run typecheck` (`vue-tsc --noEmit`): **0 errors**.
   - `pnpm run build` (`quasar build -m pwa`): **Build succeeded**.

---

## 2. What Was Completed in Previous Session (v0.9.4)
1. **Forensic Root Cause Diagnosis: Disappearing Orders Across Rounds**:
   - **Data Integrity Proved**: Order `FD-3427` (Document ID `kpbTmD3RAUzcdwr4lblF` in Firestore) was never deleted. It was created under `ROUND-001` ("รอบส่งผลไม้ วันอังคาร 8 ก.ย.").
   - **Root Cause**: The tailgate dispatch desk (`/admin` / `DispatchPage.vue`) was switched to `ROUND-604612` ("ลองกองหวาน สดจากสวน"), which had 0 orders.
   - **Auto-Selection Hijacking**: `subscribeToAllRounds` in `fruitStore.ts` was auto-selecting the newest round (`allRounds[0]` ordered by `createdAt: desc`). Whenever a new round was created, every page refresh or reload silently hijacked the admin desk to the new empty round.
2. **Admin Round Persistence & Unrestricted Dispatch Desk View**:
   - In `fruitStore.ts`, persisted active round selection in `localStorage` (`fruit_drop_admin_selected_round`) and restored it upon subscription so page refreshes never reset the active round.
   - In `DispatchPage.vue`, updated `roundOptions` to display all rounds (with `[ปิดรับแล้ว]` tag for closed rounds), allowing sellers to dispatch orders even after preorders are closed.
3. **Cross-Round Customer Order Search**:
   - In `fruitStore.ts`, added `searchOrderAcrossRounds(searchQuery)` which queries active memory first, then Firestore by exact `orderId` or phone number, with fallback to recent orders.
   - In `OrderLookupModal.vue`, updated customer search to use `searchOrderAcrossRounds` with a loading state, ensuring customers can find their order even if another round is active.
   - In `IndexPage.vue`, updated `activeCustomerOrder` to retrieve the order directly via `getOrderByOrderId` across rounds so the "มีออเดอร์รอรับ" banner is always preserved.
4. **Automated Verification**:
   - `pnpm run typecheck`: PASSED (0 errors).
   - `pnpm run build`: PASSED (0 errors).
   - Chrome CDP Test (`round_001_orders_found.png`): Confirmed `#FD-3427` instantly renders on the tailgate desk when `ROUND-001` is selected.

---

## 2. What Was Completed in Previous Session (v0.9.2)
1. **Excluded App Brand Logo from Fruit Mascot Options (`src/utils/fruitMascots.ts`)**:
   - Removed the composite app brand logo (`/mascots/logo_fruit_drop.png`) from `BUILT_IN_MASCOTS`.
   - The predefined 2.5D mascot selector in `FruitEditDialog.vue` now strictly presents only the 7 individual fruit characters: น้องเงาะ, น้องทุเรียน, น้องมังคุด, น้องลองกอง, น้องลางสาด, น้องส้ม, และน้องมะม่วง.
   - Updated default avatar initialization in `FruitEditDialog.vue` from the app logo to the default fruit mascot (`/mascots/mascot_ngo.png`).
2. **Automated Verification**:
   - `pnpm run typecheck`: PASSED (0 errors).
   - `pnpm run build`: PASSED (0 errors).
   - Automated Headless Chrome CDP test (`mascots_without_logo.png`): Confirmed `hasLogo: false` and `mascotCount: 7`.

---

## 2. What Was Completed in Previous Session (v0.9.1)
1. **Removed Ambiguous Leaf Icon Button from Header (`AdminLayout.vue`)**:
   - Removed the small `eco` icon button between role badge and logout button.
   - Eliminates accidental logout taps on mobile screens and cleans up the top bar. Access to fruit catalog is cleanly anchored in `RoundsPage.vue` ("แคตตาล็อกผลไม้") and `RoundEditPage.vue` ("+ เพิ่มชนิดผลไม้").
2. **Separated Toggle Switch & Edit Button in Master Fruit Cards (`FruitsPage.vue`)**:
   - Eliminated tight vertical stacking of "แก้ไข" and "เปิดใช้งาน" on the right side of cards.
   - Adopted the proven, ergonomic bottom-row layout from `RoundsPage.vue`:
     - Top section: Fruit Avatar, Name, Sale type badge, and Pricing/Cost/Profit details.
     - Separator: `<q-separator color="grey-3" class="q-my-sm" />`.
     - Bottom action row (`row items-center justify-between`): Left = `เปิดใช้งาน` switch (`left-label`), Right = `แก้ไขข้อมูล` outline button.
     - Maximum physical separation (350+ px on mobile) eliminates fat-finger misclicks.
3. **Streamlined Fruit Creation/Edit Dialog Header (`FruitEditDialog.vue`)**:
   - Removed the bulky leaf avatar (`q-avatar`).
   - Removed verbose subtitle ("กำหนดข้อมูลผลไม้กลาง...").
   - Removed redundant `X` close icon button (dialog already has the bottom "ยกเลิก" cancel button).
   - Saved ~70px of vertical space, allowing the live mascot selector and form fields to be immediately visible without scrolling.
4. **Automated Verification**:
   - `pnpm run typecheck`: PASSED (0 errors).
   - `pnpm run build`: PASSED (0 errors).
   - Automated Headless Chrome CDP tests:
     - `header_no_leaf.png`: Verified leaf icon button is gone (`hasLeafBtn: false`).
     - `fruits_deck_separated_actions.png`: Verified bottom row separates toggle (left) and edit button (right).
     - `dialog_compact_header.png`: Verified compact title header with 0 redundant icons (`hasCloseX: false`, `hasEcoIcon: false`).

---

## 2. What Was Completed in Previous Session (v0.9.0)
1. **Master Fruit Catalog CRUD System (`master_fruits` Firestore collection)**:
   - **Data Contract & Architecture**: Created `MasterFruit` interface in `src/types/fruit_app.ts` (`id`, `fruitKey`, `name`, `productType`, `defaultPricePerKg`, `defaultCostPerKg`, `defaultTotalQuotaKg`, `imageUrl`, `avatarType`, `bundles`, `sizeTiers`, `isActive`, `sortOrder`, `createdAt`). Widened `RoundCreationFruitConfig.fruitKey` and `ProductItem.mascotKey` to `string` to support unlimited dynamic fruits.
   - **Real-Time Pinia Store Management (`src/stores/fruitStore.ts`)**:
     - Added `masterFruits` and `activeMasterFruits` reactive state.
     - Added `subscribeToMasterFruits()` with auto-seeding of the 7 default master fruits if the collection is initially empty in Firestore.
     - Implemented `saveMasterFruit()`, `toggleMasterFruitActive()`, and `deleteMasterFruit()` (soft delete to preserve historical integrity).
     - Implemented `uploadFruitImage()` with client-side canvas compression (`imageCompressor.ts`, 600x600, 85% JPEG) uploaded to Firebase Storage (`fruits/mascot_{fruitKey}_{timestamp}.jpg`).
     - Enhanced `createRound` and `updateRound` to dynamically copy `imageUrl`, `bundles`, and `sizeTiers` from master fruits into round products (`ProductItem`).
   - **Mascot & Custom Avatar Utilities**:
     - Added `BUILT_IN_MASCOTS` catalog in `src/utils/fruitMascots.ts`.
     - Updated `getFruitMascotUrl()` to prioritize custom uploaded URLs while falling back to matched mascot files or generic logo.
     - Updated `FruitMascotAvatar.vue` with optional `imageUrl` prop.
2. **Dedicated Master Fruit Catalog Page (`/admin/fruits` - `FruitsPage.vue`)**:
   - Created full-page catalog view with M3 surface cards (`border-radius: 16px`), 2.5D mascot avatars, sale archetype badges (`ขายยกกิโล` vs `ชั่งตามลูก`), price/cost breakdown with profit calculation, edit button, and active toggle switch.
   - Wired into top header navigation (`AdminLayout.vue` with eco icon) and page header in `RoundsPage.vue` (`แคตตาล็อกผลไม้`).
3. **Reusable Fruit Creation & Edit Modal (`FruitEditDialog.vue`)**:
   - Created M3 modal supporting 2.5D mascot selector chips, custom image file uploader with preview and reset, Thai fruit name, slug input, sale archetype selection (`FIXED_WEIGHT` vs `VARIABLE_WHOLE_FRUIT`), default pricing, cost, and quota inputs, with live profit margin calculator.
4. **In-Place Quick Fruit Creation in Round Edit (`RoundEditPage.vue`)**:
   - Added `[+ เพิ่มชนิดผลไม้]` button in Section 3 header.
   - Mounted `FruitEditDialog` for in-place creation without navigating away from the round creation flow.
   - When a fruit is created, it automatically appends into `form.fruits` with `isEnabled: true`.
   - `loadRoundData` merges master fruits, existing round products, and summary list.
5. **Snapshot Isolation & Zero Historical Regression**:
   - Past rounds and past customer orders maintain their isolated product snapshots; editing master fruits never mutates past transactions.
6. **Automated Verification**:
   - `pnpm run typecheck`: PASSED (0 errors).
   - `pnpm run build`: PASSED (0 errors).
   - Headless Chrome CDP automated verification:
     - `admin_fruits_catalog.png`: Verified `/admin/fruits` renders all master fruit cards with live profit badges and toggles.
     - `admin_fruit_dialog.png`: Verified `FruitEditDialog` modal with 2.5D mascot selector and file upload.
     - `round_edit_quick_add.png` & `round_edit_sec3_view.png`: Verified Section 3 fruit list and quick-add modal in `RoundEditPage.vue`.

---

## 2. What Was Completed in Previous Session (v0.8.20)
1. **Resolved Accidental Default Fruit Selection Bug in Round Edit Mode (`RoundEditPage.vue`)**:
   - **Root Cause Forensic Diagnosis**:
     - `getDefaultFruitConfigs()` had `ngo` (เงาะโรงเรียน) and `thurian` (ทุเรียนหมอนทอง) hardcoded with `isEnabled: true`.
     - In `loadRoundData(id)`, the template mapped over `defaultFruits`. If a fruit was NOT present in the saved round's `products` collection (e.g. user only chose `longkong`), it fell through to `return df;`, leaving `df.isEnabled` as `true` for both `ngo` and `thurian`.
   - **Architectural Solution**:
     - Changed all 7 master fruit templates in `getDefaultFruitConfigs()` to `isEnabled: false`.
     - In `loadRoundData(id)`, strictly mapped `isEnabled: true` ONLY if a matching product exists in `prods` OR appears in `roundData.fruitSummary`. All other unselected fruits remain strictly `isEnabled: false`.
     - If saving in edit mode, non-selected products are deleted from Firestore so customer cannot mistakenly order unchosen fruits.
2. **Standardized 2.5D Mascot Avatars in Admin Rounds Management Cards (`RoundsPage.vue`)**:
   - Replaced old green text chips (`FruitChip.vue`) with unified `<FruitMascotAvatar size="54px">` in a wrapping flex row with `gap: 12px`.
   - Admin round cards now display visual 2.5D mascot illustrations identical to the customer storefront with zero horizontal overflow.
3. **Automated Verification**:
   - Headless Chrome CDP tested on iPhone 12 Pro (390x844):
     - `admin_round_edit_section3.png`: Verified only `longkong` is checked; `ngo` and `thurian` are unchecked (`isChecked: false`).
     - `admin_rounds_avatars.png`: Verified Card 1 (ลองกอง) and Card 2 (มังคุด, เงาะ, ทุเรียน) render avatars with bottom labels, with 0 horizontal overflow (`scrollWidth`: 390px, `cardWidth`: 358px).
   - `pnpm run typecheck`: PASSED (0 errors).
   - `pnpm run build`: PASSED (0 errors).

---

## 2. What Was Completed in Previous Session (v0.8.19)

---

## 2. What Was Completed in Previous Session (v0.8.17)
1. **Eliminated Icon & Text Separation in Round Cards (`RoundsPage.vue`)**:
   - **Forensic Diagnosis**: Round details (Date, Standby Time, Location) were previously rendered as loose children within a single wrapping flex row (`row items-center`). When space ran out on mobile viewports, the icon fit on the trailing end of line 1 while its text dropped to line 2, and the location icon fit at the end of line 2 while its text dropped to line 3.
   - **Architectural Solution**: Restructured the logistical details into atomic, dedicated rows with `no-wrap` and `flex-shrink-0` on each icon:
     - Row 1: 📅 Date (`event` icon + `round.pickupDate`)
     - Row 2: 🕒 Pickup Window (`schedule` icon + `เวลารับของ: ... น.`)
     - Row 3: 📍 Pickup Location (`place` icon + `round.pickupLocation`)
     - Row 4: 💳 Payment Credentials (`payments` icon + PromptPay & Bank Info)
   - Guarantees icons and text are permanently bound together with zero chance of awkward wrapping.
2. **Automated Verification**:
   - Headless Chrome CDP tested on iPhone 12 Pro (390x844) with long user strings:
     - `rounds_management_v2.png`: Verified perfect vertical alignment of all 4 icons with their respective text labels.
   - `pnpm run typecheck`: PASSED (0 errors).
   - `pnpm run build`: PASSED (0 errors).

---

## 2. What Was Completed in Previous Session (v0.8.16)
1. **Cleaned Up Redundant Badges & Cluttered Stacks (`RoundsPage.vue`)**:
   - Removed duplicate `RoundStatusBadge` ("เปิดรับจอง") from the card header because the bottom toggle switch already clearly displays and controls the live booking status.
   - Removed `กำลังดูที่ท้ายรถ` badge from the card header, eliminating awkward vertical badge stacking on long round titles.
2. **Removed Superfluous "ดูออเดอร์" Button (`RoundsPage.vue`)**:
   - Removed the "ดูออเดอร์" action button from round cards.
   - Rounds management page is now 100% focused on its core responsibility: configuring round details and toggling booking visibility. Round selection for dispatch is handled natively via the in-place Round Selector on the Tailgate Desk.
3. **Filtered Round Selector to Active/Open Rounds Only (`DispatchPage.vue`)**:
   - `roundOptions` now strictly filters for rounds with `isOpen === true`.
   - Tailgate desk selector now only exposes open rounds for active fulfillment.
   - Updated dropdown slot styling to clean typography without redundant badges.
4. **Synchronized Active Round Auto-Selection (`fruitStore.ts`)**:
   - In `subscribeToAllRounds()`, automatically prioritizes open rounds when selecting or syncing `activeRound`.
5. **Automated Verification**:
   - Headless Chrome CDP tested on iPhone 12 Pro (390x844):
     - `rounds_management_v2.png`: Clean, uncluttered round card with title, details, booking switch, and edit button verified.
     - `dispatch_round_dropdown.png`: Round selector verified to display ONLY open rounds.
   - `pnpm run typecheck`: PASSED (0 errors).
   - `pnpm run build`: PASSED (0 errors).

---

## 2. What Was Completed in Previous Session (v0.8.15)
1. **Interactive Round Selector on Tailgate Desk (`DispatchPage.vue`)**:
   - Replaced static banner and "เปลี่ยนรอบ" navigation jump with an in-place `<q-select>` round switcher.
   - Sellers can now switch rounds directly at the desk in 1 tap, automatically updating active orders, KPIs, and time slots.
   - Option slots show round title, pickup date, and pill status badge (`เปิดรับจอง` vs `ปิดรับจอง`).
   - Added shortcut button (`to="/admin/rounds"`) for full rounds configuration.
2. **Disambiguated Round Management UI (`RoundsPage.vue`)**:
   - Completely eliminated the confusing dynamic button label "รอบปัจจุบัน".
   - Decoupled State from Action:
     - State Indicator: `<q-badge>` with `กำลังดูที่ท้ายรถ` displayed prominently at the top of the currently active round card.
     - Action Command: Button is standardized to **`ดูออเดอร์`** (`inventory_2` icon) across all cards, clearly indicating navigation to the tailgate dispatch view.
     - Customer Booking Toggle: Remains cleanly labeled **`เปิดรับจอง`** (`round.isOpen`), controlling storefront visibility.
3. **Resilient Active Round Auto-Initialization (`fruitStore.ts`)**:
   - In `subscribeToAllRounds()`, automatically defaults `activeRound` to the latest round if none is active or if the active round is deleted/modified, preventing blank desk states.
4. **Automated Verification**:
   - Headless Chrome CDP tested on iPhone 12 Pro (390x844):
     - `dispatch_round_selector.png`: Round selector on tailgate desk verified.
     - `dispatch_round_dropdown.png`: Dropdown menu with status badges verified.
     - `rounds_management_v2.png`: Clean rounds cards with `กำลังดูที่ท้ายรถ` badge and uniform `ดูออเดอร์` button verified.
   - `pnpm run typecheck`: PASSED (0 errors).
   - `pnpm run build`: PASSED (0 errors).

---

## 2. What Was Completed in Previous Session (v0.8.14)
1. **Removed Device Telemetry ("อุปกรณ์: ...")**:
   - Stripped out `lastLoginDevice` from the user deck list items in `UsersPage.vue`.
2. **Centralized Role Metadata (`src/utils/roles.ts`)**:
   - Created single source of truth for all role-related constants, dropdown options, and colors (`ROLES` object: `Admin`, `Owner`, `Seller`).
   - Implemented shared helpers `getRoleShortLabel()`, `getRoleFullLabel()`, and `getRoleColor()`.
   - Replaced scattered inline ternaries in `AdminLayout.vue` and long Thai strings in `UsersPage.vue` with short unified badges (`Admin`, `Owner`, `Seller`).
3. **Eliminated Placeholder Avatars**:
   - Removed `<q-avatar>` circles completely from `UsersPage.vue`, eliminating redundant clutter when no profile photo uploads exist and recovering 56px of horizontal space.
4. **Added User Edit Capability (`UsersPage.vue`)**:
   - Added Edit button on user list rows for editable accounts.
   - Unified modal dialog (`showUserDialog`) supporting both Add and Edit operations.
   - When editing: Gmail input is locked/readonly, modal title shows "แก้ไขข้อมูลผู้ใช้งาน", pre-filled with existing data, and saves via `userStore.updateUser()`.
5. **Automated Verification**:
   - Headless Chrome CDP tested on iPhone 12 Pro (390x844):
     - `users_deck_v2.png`: Shows clean deck without avatars/device telemetry, unified short role badges, edit/delete actions.
     - `users_edit_dialog.png`: Shows edit modal populated with existing user information and readonly Gmail.
   - `pnpm run typecheck`: PASSED (0 errors).
   - `pnpm run build`: PASSED (0 errors).

---

## 2. What Was Completed in Previous Session (v0.8.13)
1. **Standardized Global Page Padding & Max-Width Architecture**:
   - **Forensic Diagnosis**: Admin pages previously exhibited mismatched padding (`q-pa-sm q-pa-md-md` vs `q-pa-md`) and inconsistent max-widths (`780px` in Analytics vs `680px` in Dispatch/Users vs `600px` in QR Scanner), causing jarring lateral jumping when switching tabs.
   - **Architectural Solution**: Enforced uniform container metrics across all admin views:
     - `class="q-pa-md bg-grey-1 text-grey-9" style="max-width: 680px; margin: 0 auto; padding-bottom: 84px;"` (synchronized on `DispatchPage.vue`, `RoundsPage.vue`, `RoundEditPage.vue`, `AnalyticsPage.vue`, `UsersPage.vue`, `QrScannerPage.vue`, and `OrderDetailPage.vue`).
     - Customer pages unified on `max-width: 620px; padding-bottom: 90px;`.
2. **Unified Single Surface Sheet for Order Fulfillment (`admin/orders/{id}`)**:
   - Eliminated the 6 fragmented, floating cards.
   - Consolidated the entire order fulfillment dossier into **one single white Surface Card** (`<q-card class="bg-white shadow-1 rounded-borders overflow-hidden">`).
   - Clean horizontal hairpins (`<q-separator />`) cleanly partition Header, Customer Info (flat, zero subcards), Items & Durian Scale, Payment QR, Photo Proof, and Handover Commands.
3. **AppSheet-Style Team Member Deck View & On-Demand Modal (`admin/users`)**:
   - Removed the cluttered inline user creation form from the main page view.
   - Elevated user creation to an on-demand `<q-dialog v-model="showAddUserDialog">` modal triggered via a prominent `+ เพิ่มผู้ใช้งาน` action button.
   - Converted the user list into an edge-to-edge AppSheet Deck view (`<q-card class="overflow-hidden">` + `<q-list separator>`) with role-colored avatars, pill badges, clickable `tel:` links, login device telemetry, and delete actions.
4. **Flush Deck List for Round Fruit Pricing Config (`admin/rounds/{id}/edit`)**:
   - Refactored Card 3 from floating `.bg-grey-1` subcards into a native flush `<q-list separator>`.
5. **Automated Verification**:
   - Headless Chrome CDP tested on iPhone 12 Pro (390x844):
     - `users_deck_view.png` & `users_add_dialog.png` verified.
     - `order_detail_sheet.png` & `order_detail_sheet_lower.png` verified.
     - `analytics_standard_padding.png` verified.
   - `pnpm run typecheck`: PASSED (0 errors).
   - `pnpm run build`: PASSED (0 errors).

---

## 2. What Was Completed in Previous Session (v0.8.12)
1. **Elimination of Card-in-Card Desktop Anti-Pattern (`AnalyticsPage.vue`)**:
   - **Root Cause & Architectural Shift**:
     - On mobile screens (360px–390px), horizontal real estate is precious. Nesting cards inside outer cards with layered padding and margins consumed over 80px of horizontal width, squishing typography and metrics.
     - Following native mobile deck list standards (e.g. ShopeePay, AppSheet, YouTube mobile) and Quasar best practices, restructured the UI to use a single surface `<q-card class="overflow-hidden">` containing edge-to-edge flush `<q-list separator>`.
   - **Component Refactoring**:
     - **Page Container**: Set `<q-page>` padding to `class="q-pa-sm q-pa-md-md"` to grant maximum edge breathing room on mobile viewports.
     - **Fruit Breakdown Analysis ("กำไรตามชนิดผลไม้")**:
       - Replaced floating subcards with flush `<q-list separator>`.
       - Added distinct header bar (`<div class="q-px-md q-py-sm row items-center justify-between">`) separated by `<q-separator />`.
       - List rows now span 100% of the card width with generous internal padding (`q-pa-md`).
     - **Order-Level Profit Breakdown ("สรุปรายออเดอร์")**:
       - Replaced floating rounded cards with flush `<q-list separator>` and native `<q-expansion-item header-class="q-pa-md" dense-toggle>`.
       - Structured header into 2 full-width flex rows (`Row 1`: ID/Name + Profit; `Row 2`: Location/Slot + Revenue/Margin).
       - Expanded details now render as a clean flat inset block (`<div class="bg-grey-1 text-grey-9 q-pa-md border-top-light">`) without any nested rounded card.
2. **Automated Verification**:
   - Headless Chrome CDP tested on iPhone 12 Pro (390x844) and Samsung Galaxy (360x800).
   - Confirmed zero horizontal overflow (`isOrderOverflowing: false`, expansion width: 372px inside 374px container), spacious typography, and no awkward text wrapping.
   - `pnpm run typecheck`: PASSED (0 errors).
   - `pnpm run build`: PASSED (0 errors).

---

## 2. What Was Completed in Previous Session (v0.8.11)
1. **Root Cause Resolution: Order Analysis Expansion Item Breaking Outside Container (`AnalyticsPage.vue`)**:
   - **Forensic Diagnosis**:
     - The parent wrapper of the expansion items used `<div v-else class="column">`. In Quasar, `.column` applies `display: flex; flex-direction: column; flex-wrap: wrap;`. Because of `flex-wrap: wrap;`, children on the cross-axis (X-axis) size according to their content width rather than being constrained to 100% of the parent card.
     - Inside `<q-expansion-item>`, the main section's rows lacked width constraints (`full-width`) and their flex children lacked `min-width: 0` / `col`, causing Row 2 to request 257.7px intrinsic width. When added to Avatar (56px) + Toggle (40px) + Padding (32px) + Border (2px), the expansion item grew to 387.7px, blowing 30px past the parent card's right boundary (358px on iPhone 12 Pro 390px viewport).
   - **Architectural Solution**:
     - Removed `class="column"` from the parent container (`<div v-else>`), restoring native block width behavior (`width: 100%`).
     - Added `dense-toggle` and compact avatar spacing (`style="min-width: 42px; padding-right: 8px;"`), gaining 42px of extra horizontal space for content.
     - Added `full-width` to both Row 1 and Row 2, and added `col` class to the left text items so they properly flex-shrink and respect the bounds.
     - Formatted Row 2 right text cleanly to `ขาย ฿{{ order.revenue.toLocaleString() }} ({{ order.marginPercent }}%)`, ensuring full customer location and pickup slot visibility without truncation.
2. **Automated Verification**:
   - Built and ran headless Chrome CDP test script (`verify_live_fix.mjs`) on iPhone 12 Pro (390x844) viewport.
   - Live measurements confirmed: `isOverflowingParent: false`, `cardRight: 357` <= `parentRight: 374` (17px safe margin inside parent card), `mainSecWidth: 224px`, `isOverlappingToggle: false`.
   - Verified both collapsed state (`live_fix_collapsed.png`) and expanded state (`live_fix_expanded.png`).
   - `pnpm run typecheck`: PASSED (0 errors).
   - `pnpm run build`: PASSED (0 errors).

---

## 2. What Was Completed in Previous Session (v0.8.10)
1. **Restructured Order Breakdown Header into 2 Full-Width Rows (`AnalyticsPage.vue`)**:
   - **User Feedback & Root Cause**: In the Admin Analytics breakdown section ("สรุปรายออเดอร์"), the previous header split the content horizontally using `<q-item-section>` (left) and `<q-item-section side class="text-right">` (right). Inside `<q-expansion-item>`, Quasar restricts side sections to narrow flex-basis widths (~130px on 360-412px viewports), causing the order ID, customer name, location, and margin numbers to wrap into 6 cramped micro-lines.
   - **Architectural Solution**:
     - Removed `<q-item-section side>`, consolidating all header content into a single `<q-item-section>` that spans 100% of the horizontal space between the left avatar and right expand icon.
     - Implemented 2 distinct, full-width flexbox rows:
       - **Row 1** (`row items-center justify-between no-wrap`):
         - Left: `#{{ order.orderId }} - {{ order.customer.name }}` (`text-subtitle2 text-weight-bolder text-grey-9 ellipsis`)
         - Right: `+฿{{ order.profit.toLocaleString() }}` (`text-subtitle2 text-weight-bolder text-no-wrap text-positive`)
       - **Row 2** (`row items-center justify-between no-wrap text-caption text-grey-7 q-mt-xs`):
         - Left: `{{ order.customer.shop }} ({{ order.customer.floor }}) | {{ order.pickupSlot }}` (`ellipsis`)
         - Right: `ขาย ฿{{ order.revenue.toLocaleString() }} (มาร์จิ้น {{ order.marginPercent }}%)` (`text-no-wrap text-right`)
     - Eliminated word-breaking and awkward line squishing on all screen sizes.
2. **Automated Verification**:
   - Built and ran headless Chrome CDP test script (`verify_analytics_2rows.mjs`) generating screenshot `scratch/test_analytics_2rows.png`, proving 2 spacious, clean rows with no text wrapping.
   - `pnpm run typecheck`: PASSED (0 errors).
   - `pnpm run build`: PASSED (0 errors).

---

## 2. What Was Completed in Previous Session (v0.8.9)
1. **Migration to Single-Time Pickup Slots & Admin Time Filter**:
   - **Forensic Diagnosis**: The customer checkout flow was upgraded to select a single arrival time (e.g. `19:00 น.`, `19:30 น.`), but the Admin Dispatch desk (`DispatchPage.vue` & `TimeSlotTabs.vue`) and round documents in Firestore still relied on old time-range strings (`19:00 - 19:30`, `19:30 - 20:00`), causing tab filtering and badge counters to fail to match customer orders.
   - **Architectural Solution**:
     - Created dedicated time utility module `src/utils/timeSlots.ts` (`generateTimeSlots`, `normalizeSlotLabel`, `isRangeSlot`, `parseTimeToMinutes`).
     - Refactored `src/pages/admin/DispatchPage.vue`:
       - `availableSlots` computes chronological 30-min single-time intervals from the round's standby window (`19:00 น.`, `19:30 น.`, `20:00 น.`, etc.) and seamlessly merges any custom times chosen by customers.
       - Normalized slot counts and order filtering matching against `normalizeSlotLabel(o.pickupSlot)`.
     - Refactored `src/pages/IndexPage.vue` `availablePickupSlots` using `generateTimeSlots()`.
     - Refactored `src/pages/admin/RoundEditPage.vue` to store and initialize 30-min single time slots in `pickupSlots`.
     - Refactored `src/stores/fruitStore.ts` `seedMasterData()`.
     - Updated active round `rounds/ROUND-001` in Firestore with `standbyStartTime: "19:00"`, `standbyEndTime: "23:00"`, and single-time `pickupSlots`.
2. **Purged Legacy Time-Range Orders from Firestore**:
   - Deleted 4 legacy test orders with old range format from Firestore: `FD-1001`, `FD-1002`, `FD-1003`, `FD-1004`.
   - Preserved valid single-time order `#FD-3427` (`19:30 น.`).
3. **Automated Verification**:
   - Built and ran headless Chrome CDP script (`verify_admin_dispatch_tabs.mjs`), confirming all tabs now render single-time slots (`19:00 น.`, `19:30 น.`, etc.) with accurate badge count on `#FD-3427` (`19:30 น.`).
   - `pnpm run typecheck` (`vue-tsc --noEmit`): PASSED (0 errors).
   - `pnpm run build` (`quasar build` SPA): PASSED (0 errors).
   - `pnpm exec quasar build -m pwa`: PASSED (0 errors).

---

## 2. What Was Completed in Previous Session (v0.8.8)
1. **Root Cause Resolution: Active Order Card & Banner Inconsistent Border-Radius**:
   - **Forensic Diagnosis**: In `src/pages/IndexPage.vue`, the active customer order alert banner was rendered as `<q-banner rounded>`. In Quasar, `.q-banner--rounded` hardcodes `border-radius: $generic-border-radius (4px)`, while cards in the application use `border-radius: 16px`. This created a sharp 4px vs 16px corner mismatch. Additionally, several empty states used `<div>` with `.rounded-borders` (4px) instead of `<q-card>`.
   - **Architectural Fix**:
     - Converted active order alert banner in `src/pages/IndexPage.vue` into a semantic `<q-card id="card-active-customer-order">` with responsive flexbox layout ensuring the order info and action button never clip or wrap awkwardly.
     - In `src/css/app.scss`:
       - Normalized `.q-banner--rounded, .q-banner.rounded { border-radius: 16px !important; }` so any banner globally adheres to the 16px card border token.
       - Normalized `.rounded-borders { border-radius: 12px !important; }` for nested sub-containers.
       - Refined card gradient scoping: `.q-card:not([class*="bg-"]) { background: $gradient-surface-card; }` to prevent specificity collisions with colored cards.
     - Converted 5 empty state pseudo-divs to semantic `<q-card>` across `customer/OrderDetailPage.vue`, `ActiveRoundsList.vue`, `admin/DispatchPage.vue`, `admin/RoundsPage.vue`, and `admin/OrderDetailPage.vue`.
     - Codified explicit **M3 Expressive Shape Hierarchy** rules in both `AGENTS.md` and `GEMINI.md` (Surface Cards & Banners: 16px, Nested containers: 10px-12px, Buttons/Badges: 9999px pill).
   - **Automated Verification**: Ran headless Chrome CDP script (`verify_border_radius.mjs`), confirming computed `borderRadius: "16px"` on `#card-active-customer-order` with verified screenshot.
2. **Verification**:
   - `pnpm run typecheck` (`vue-tsc --noEmit`): PASSED (0 errors).
   - `pnpm run build` (`quasar build` SPA): PASSED (0 errors).
   - `pnpm exec quasar build -m pwa`: PASSED (0 errors).

---

## 2. What Was Completed in Previous Session (v0.8.7)
1. **Replaced Mobile Order Ticket Modals with Dedicated Full-Page Route**:
   - **User Feedback & Issue**: Modal dialogs on mobile cause nested scrolling friction, clipping, and fail to integrate with mobile hardware/gesture Back navigation.
   - **Implementation**:
     - Created `src/pages/customer/OrderDetailPage.vue` under the `/orders/:orderId` route.
     - Top navigation bar with native `handleBack()` (`window.history.length > 1 ? router.back() : router.push('/')`) and pickup slot indicator.
     - Embedded full `<OrderQueueCard :order="order" />` with authentic mascot image, itemized fruit, compact QR code, and PNG download button.
     - Bottom navigation action: `[ สั่งจองผลไม้เพิ่ม ]` returning to `/`.
     - Registered route in `src/router/routes.ts`: `{ path: 'orders/:orderId', name: 'customer-order-detail', component: () => import('@/pages/customer/OrderDetailPage.vue') }`.
     - Removed all order ticket modal dialogs from `src/pages/IndexPage.vue` (`showSuccessModal`, `showActiveQueueModal`) and `src/layouts/MainLayout.vue` (`isQueueModalOpen`).
     - Order submission redirects directly via `router.push('/orders/' + generatedOrderId)`.
     - Order lookup from navbar redirects directly via `router.push('/orders/' + order.orderId)`.
2. **Standardized Action Wording to "รายละเอียดคำสั่งซื้อ"**:
   - Replaced old button label `ดูบัตรคิว` with `รายละเอียดคำสั่งซื้อ` on the storefront top banner in `IndexPage.vue`.
   - Updated lookup modal headings and descriptions in `OrderLookupModal.vue` to reference `ค้นหารายละเอียดคำสั่งซื้อ` and `ดูรายละเอียดคำสั่งซื้อ`.
3. **Verification**:
   - `pnpm run typecheck` (`vue-tsc --noEmit`): PASSED (0 errors).
   - `pnpm run build` (`quasar build` SPA): PASSED (0 errors).
   - `pnpm exec quasar build -m pwa`: PASSED (0 errors).

---

## 2. What Was Completed in Previous Session (v0.8.6)
1. **Root Cause Resolution: Blank White Exported Image**:
   - **Forensic Diagnosis**: In v0.8.5, an attempt to calculate tight card dimensions using an off-screen clone with `clone.style.left = '-9999px'` caused `html-to-image`'s SVG `<foreignObject>` to render the content at X = -9999px, completely outside the visible (0..width) viewport. The resulting image was a 100% blank white canvas (width 396 x height 1024, 11 KB).
   - **Fix**: Replaced the offscreen clone with an in-place collapse mechanism in `src/utils/orderTicketCanvas.ts`.
     - Elements marked `.hide-on-capture` are temporarily collapsed via `el.style.display = 'none'`.
     - Forced layout reflow via `void cardElement.offsetHeight`.
     - `html-to-image` captures the live card in the natural document flow with exact tight dimensions and authentic styling.
     - In the `finally` block, all `.hide-on-capture` elements are restored with `el.style.display = ''`.
   - **Automated Verification**: Built and ran an automated headless Chrome CDP test script (`test_export.mjs`) on the live local development server (`http://localhost:9000/#/`), successfully exporting a crisp 63.6 KB non-blank PNG with authentic mascot branding, QR code, and tight proportions.
2. **Verification**:
   - `pnpm run typecheck` (`vue-tsc --noEmit`): PASSED (0 errors).
   - `pnpm run build` (`quasar build` SPA): PASSED (0 errors).
   - Headless CDP Render Test: PASSED (63,601 bytes, perfectly rendered image).

---

## 2. What Was Completed in Session (v0.8.5)
1. **Purged Web Share API in Favor of Direct File Download Everywhere**:
   - **Root Cause**: `navigator.share` invokes the OS share sheet (Windows flyout or mobile share sheet) which interrupts saving. The customer intent is to **download/save the order ticket** directly into their device, not to share it with another app or contact.
   - **Fix**: Removed `navigator.share` and `isMobileDevice` completely in `src/utils/orderTicketCanvas.ts`. `deliverOrderTicketBlob()` now triggers `<a download="FruitDrop-Order-FD-xxxx.png">` unconditionally across all devices (PC, macOS, iOS Safari, Android Chrome).
2. **Fixed Oversized Empty Whitespace Below QR Code in Exported Order Ticket**:
   - **Root Cause**: In `OrderQueueCard.vue`, the action buttons were nested inside the grey QR container (`<div class="bg-grey-2">`). In addition, `html-to-image` computes the SVG foreignObject canvas dimensions and CSS styles from the live DOM element (which included the button height). When filtered out, the clone preserved the full height with empty space.
   - **Fix**:
     - Separated the action buttons into their own sibling container (`<div class="q-mb-md hide-on-capture">`) outside the compact grey QR container (`bg-grey-2 q-pa-md rounded-borders text-center q-mb-sm`).
     - In `exportOrderTicket()`, created an offscreen DOM clone attached to `document.body` (`position: fixed; left: -9999px; width: ${cardElement.offsetWidth}px`) and removed `.hide-on-capture` before invoking `toBlob()`.
     - The clone's height is measured naturally without action buttons, resulting in a tight, beautifully proportioned order pass with zero phantom whitespace and zero UI layout jump on screen.
     - Adjusted the fallback Canvas generator (`generateOrderTicketCanvas`) `qrBoxHeight` from 410 to 330 for identical tight proportioning.
3. **Purged All Remaining AI Fluff & Verbose Wording**:
   - Button label simplified to `บันทึกรูปคำสั่งซื้อ` with `icon="download"`.
   - Caption simplified to `ใช้เพื่อแสดงตอนรับสินค้า` (removed emojis `📸`, `💾`, `💡`, and redundant text like `(ลงมือถือ)`, `(กันลืม)`).
   - Top banner subtitle: `สั่งจองผลไม้ • บัตรคิวรับของ` | Status: `สั่งจองผลไม้สำเร็จ`.
   - Footer note: `แสดงภาพนี้เพื่อรับสินค้าท้ายรถ`.
4. **Verification**:
   - `pnpm run typecheck` (`vue-tsc --noEmit`): PASSED (0 errors).
   - `pnpm run build` (`quasar build` SPA): PASSED (0 errors).

---

## 2. What Was Completed in Session (v0.8.3)
1. **High-Resolution Order Ticket Canvas Image Export**:
   - Built native HTML5 Canvas 2D engine in `src/utils/orderTicketCanvas.ts` generating a complete, crisp 750px Retina order pass.
   - Includes Brand Banner, Status Badge, Order ID `#FD-xxxx`, 220px embedded QR Code, Customer profile (Name, Shop, Floor, Phone), itemized fruit list with bundles/tiers, Pickup Slot & Location, and Payment method/Total.
   - Integrated mobile Web Share API (`navigator.share({ files: [file] })`) so iOS Safari and Android Chrome users can save directly to Photos/Gallery or share via LINE, with seamless fallback to anchor download.
2. **Prominent Order Ticket Save Button in `OrderQueueCard.vue`**:
   - Added eye-catching `.btn-gradient-primary` button: `📸 บันทึกรูปใบออเดอร์ลงมือถือ (มี QR)` with `id="btn-save-full-order-ticket"` and `data-audit-id="btn-save-full-order-ticket"`.
   - Added reassuring subtitle: *"💡 แนะนำให้บันทึกรูปเก็บไว้ในมือถือเพื่อแสดงตอนรับของที่รถ (กันลืม)"*.
   - Completely solves the no-login customer experience: customer keeps the complete order ticket in their photo gallery.
3. **Verification**:
   - `pnpm run typecheck` (`vue-tsc --noEmit`): PASSED (0 errors).
   - `pnpm run build` (`quasar build` SPA): PASSED (0 errors).
   - `pnpm quasar build -m pwa`: PASSED (0 errors).

---

## 2. What Was Completed in Session (v0.8.2)
1. **SCSS Specificity Bug Fix (Card Background Clobbering)**:
   - Removed `!important` from `background: $gradient-surface-card` on `.m3-surface-card, .q-card:not(.q-card--square)` in `src/css/app.scss`.
   - Fixed bug where selector specificity `(0, 2, 0)` with `!important` was clobbering Quasar's background color utility classes (such as `.bg-green-1` on the active round banner on `DispatchPage.vue`).
2. **Global Border Utilities & CSS Deduping (Ironclad Rules 1 & 3)**:
   - Defined `.border-light`, `.border-top-light`, `.border-positive`, `.border-amber`, `.border-positive-subtle`, and `.border-negative-subtle` globally in `src/css/app.scss`.
   - Fixed un-styled `.border-positive` and `.border-amber` in `TailgateOrderCard.vue` and `OrderDetailPage.vue`.
   - Purged duplicated `<style scoped>` blocks with hardcoded hex values in `UsersPage.vue`, `RoundEditPage.vue`, `AnalyticsPage.vue`, and `PickupSlotPicker.vue`, reducing compiled CSS chunks from 8 to 4.
3. **100% Elimination of Robotic "กรุณา" in Thai UX Copywriting**:
   - Replaced all residual robotic "กรุณา" occurrences across `OrderDetailPage.vue`, `UsersPage.vue`, and `OrderLookupModal.vue` with concise, natural Thai ("ตรวจสอบ...", "ระบุ...").
4. **Audit-Friendly Element Identification (Rule 10)**:
   - Added unique `id` and `data-audit-id` attributes to converted semantic `<q-card>` elements: `round-booking-header-card`, `order-action-card`, `dispatch-summary-kpi-card`, and `dispatch-active-round-banner`.
5. **Verification**:
   - `pnpm run typecheck` (`vue-tsc --noEmit`): PASSED (0 errors).
   - `pnpm run build` (`quasar build` SPA): PASSED (0 errors, 4 CSS files, 37 JS chunks).

---

## 2. What Was Completed in Session (v0.8.1)
1. **Google M3 Expressive Standards & Design Tokens**:
   - In `src/css/quasar.variables.scss`: Added `$button-border-radius: 9999px;` and `$button-rounded-border-radius: 9999px;` for full capsule action buttons.
   - In `src/css/app.scss`:
     - Updated card rule: `.m3-surface-card, .q-card:not(.q-card--square) { border-radius: 16px !important; border: 1px solid rgba(0, 0, 0, 0.08); background: $gradient-surface-card; overflow: hidden; }`
     - Normalized `.q-badge`: `border-radius: 9999px; padding: 3px 8px; font-weight: 600; letter-spacing: 0.2px;`
     - Added nested sub-surface helper: `.surface-subcard, .sub-row-item { border-radius: 10px; }`
2. **Component & Page Semantic Cards Structure**:
   - `src/pages/IndexPage.vue`: Converted top return bar and bottom total order container from `<div>` to `<q-card>`.
   - `src/pages/admin/DispatchPage.vue`: Converted top summary KPI card and active round banner from `<div>` to `<q-card>`.
   - `src/pages/admin/OrderDetailPage.vue`: Converted handover action box from `<div>` to `<q-card>`.
   - `src/components/admin/TimeSlotTabs.vue`: Added `rounded` prop to search `<q-input>` for M3 capsule appearance.
   - `src/pages/admin/UsersPage.vue`: Applied `surface-subcard` (10px) to user list rows.
3. **Thai UX Copywriting Clean-up (Eliminated AI Filler & Robotic Wording)**:
   - Purged unnatural robotic AI phrasing ("กรุณา", "Standby", "ผู้รับผลไม้") in favor of natural conversational Thai:
     - `IndexPage.vue`: "มีออเดอร์รอรับ:", "ไม่ต้องโอนล่วงหน้า รับของแล้วค่อยจ่ายเงินสดหรือสแกน QR กับคนขาย", "อยู่นอกเวลาส่งของคนขาย โปรดเลือกเวลาใหม่", "สั่งเพิ่มอีกออเดอร์", removed "กรุณา" across validations.
     - `ActiveRoundsList.vue`: "ยังไม่มีรอบเปิดจอง", "รอรอบถัดไป หรือค้นหาออเดอร์ที่สั่งไว้", "ค้นหาออเดอร์", "รอบที่เปิดรับจอง ({{ rounds.length }}):".
     - `FruitSelector.vue`: "เลือกผลไม้", "ราคาประมาณ: {{ min }} - {{ max }} บาท", "ชั่งน้ำหนักและคิดเงินจริงตอนรับของที่รถ", "ยกเลิกรายการนี้".
     - `OrderQueueCard.vue`: "แคปหน้าจอไว้แสดงตอนรับของที่รถ", removed redundant promotional copy.
     - `PickupSlotPicker.vue`: "เวลานัดรับของ", "หรือระบุเวลานัดรับเอง *", removed "กรุณา".
     - `ContactPicker.vue`: "ระบุชื่อหรือชื่อเล่น", "ระบุชื่อร้านหรือบูธ", "ระบุเบอร์โทรศัพท์".
     - `OrderDetailPage.vue`: "ข้อมูลลูกค้า", "รายการผลไม้", "⚖️ ชั่งน้ำหนักทุเรียน:", "QR รับเงิน", "แนบรูปสลิป / ส่งมอบ (ถ้ามี)", "ยกเลิกการส่งมอบ", simplified durian alert.
     - `QrScannerPage.vue`: "โปรดอนุญาตให้ใช้งานกล้อง", "สแกน QR Code ลูกค้า", placeholder="เช่น FD-1004".
     - `AnalyticsPage.vue`: "กำไรตามชนิดผลไม้", "สรุปรายออเดอร์", "ขายได้: {{ ... }} กก.", removed English translation brackets `(All)`, `(Completed)`, `(Waiting Pickup)`, `(Cancelled)`, `(Cash)`, `(Transfer)`.
     - `RoundsPage.vue` & `RoundEditPage.vue`: "จัดการรอบส่ง", "เวลารับของ:", "ช่วงเวลาส่งของที่รถ *", removed "Standby" and "กรุณา".
     - `BatchHeaderCard.vue`: "ช่วงเวลาส่งของที่รถ: ...".
     - `AdminLayout.vue`: Cleaned up auth alert and removed " (Logout)".
4. **Dead Code Elimination**:
   - Removed 11 orphaned/unused files: `AdminHeaderBar.vue`, `AdminPage.vue`, `HarvestPage.vue`, `DurianScaleModal.vue`, `HarvestSummaryModal.vue`, `PaymentProofModal.vue`, `RoundManagementModal.vue`, `UserManagementModal.vue`, `PaymentMethodPicker.vue`, `EssentialLink.vue`, `SecondPage.vue`.
   - Purged all residual `q-gutter` usages in favor of Quasar margin/padding utilities (Ironclad Rule 4).
5. **Verification**:
   - `pnpm run typecheck` (`vue-tsc --noEmit`): PASSED (0 errors).
   - `pnpm run build` (`quasar build` SPA): PASSED (0 errors).

---

## 2. What Was Completed in Session (v0.8.0)
1. **Centralized Status Engine (Single Source of Truth)**:
   - Built `src/constants/status.ts` defining unified, typed status configurations (`ORDER_STATUS_CONFIGS`, `ROUND_STATUS_CONFIGS`) and resolvers (`getOrderStatusConfig`, `getRoundStatusConfig`).
   - Eradicated fragmented hardcoded labels (e.g. "ส่งมอบแล้ว" vs "ส่งมอบเรียบร้อย").
2. **Universal Google M3 Components**:
   - `src/components/common/OrderStatusBadge.vue`: Reusable pill status badge with M3 tonal gradient background, micro-border, and Material icons (`check_circle`, `payments`, `account_balance_wallet`, `scale`, `schedule`, `cancel`).
   - `src/components/common/RoundStatusBadge.vue`: Clean M3 pill badge replacing emojis (`🟢` / `⚪`).
   - `src/components/common/FruitChip.vue`: Standardized fruit summary chip with uniform typography, padding, and tonal outline, eliminating the tiny unreadable `size="sm"` chips in `RoundsPage.vue`.
3. **Resolved Status Contradictions & Revert Logic**:
   - **#FD-1002 Root Cause Fix**: Re-seeded live Firestore data to enforce `paymentMethod: 'PAY_AT_CAR'` and `paymentStatus: 'UNPAID'`. An undelivered order can no longer falsely claim "โอนเงินแล้ว" before delivery.
   - **Gated Payment QR**: In `OrderDetailPage.vue`, dynamically hides the payment QR card once the order is `COMPLETED`, preventing confusion.
   - **Airtight `doRevert()`**: Resetting delivery status now resets `orderStatus: 'WAITING_PICKUP'`, `paymentStatus: 'UNPAID'`, `completedAt: undefined`, `paidAt: undefined`, and `attribution: undefined`.
4. **Removed Unused "ยอดเก็บ" (Harvest) Module**:
   - Removed tab from bottom navigation in `src/layouts/AdminLayout.vue`.
   - Removed route `/admin/harvest` from `src/router/routes.ts`.
5. **2026 Google I/O Material Design (M3 Expressive) Gradients & Surface Architecture**:
   - Defined `$gradient-primary`, `$gradient-warning`, `$gradient-surface-card`, and tonal badge gradients in `src/css/quasar.variables.scss` and `src/css/app.scss`.
   - Cards now use M3 Surface styling with 16px corner radius and subtle 1px border.
   - Handover buttons now use `.btn-gradient-primary` and `.btn-gradient-warning`.
6. **Verification**:
   - `pnpm run typecheck` (`vue-tsc --noEmit`): PASSED (0 errors).
   - `pnpm run build` (`quasar build` SPA): PASSED (0 errors).

---

## 2. What Was Completed in Session (v0.7.5)
1. **Global Soft Badge & Chip Outline Architecture**:
   - **Root Cause & Visual Defect**: Light-tinted pastel badges and chips (`green-1`, `grey-2`, `grey-3`, `amber-1`, etc.) suffered from a near 1:1 contrast ratio against white cards (`#FFFFFF`), causing edges to wash out and making fruit tags in `ActiveRoundsList.vue` hard to distinguish.
   - **Global Design Tokens**:
     - Defined semantic tokens in `src/css/quasar.variables.scss` (`$badge-soft-primary-border`, `$badge-soft-warning-border`, `$badge-soft-negative-border`, `$badge-soft-info-border`, `$badge-soft-grey-border`).
   - **Zero-Touch Global SCSS Enhancement**:
     - In `src/css/app.scss`, defined automatic 1px outline rules for all `.q-badge` and `.q-chip` elements with light background classes (`bg-green-1/2`, `bg-grey-1/2/3/4`, `bg-amber-1/2`, `bg-orange-1/2`, `bg-red-1/2`, `bg-blue-1/2`, `bg-purple-1/2`, `bg-teal-1/2`).
     - Provided explicit reusable semantic classes: `.badge-soft-primary`, `.badge-soft-positive`, `.badge-soft-secondary`, `.badge-soft-warning`, `.badge-soft-negative`, `.badge-soft-info`, `.badge-soft-grey`.
     - Added universal dynamic outline helper: `.badge-outline-soft`, `.chip-outline-soft`, `.outline-soft` (utilizing CSS `color-mix(in srgb, currentColor 35%, transparent)`).
   - **Immediate 360° Benefits Across App**:
     - Customer storefront (`ActiveRoundsList.vue`): Fruit summary chips now have a clean, crisp green outline defining their pill shapes on white cards.
     - Pickup slot picker (`PickupSlotPicker.vue`): Unselected time suggestion chips (`grey-2`) now stand out neatly.
     - Tailgate dispatch desk (`TailgateOrderCard.vue`, `OrderDetailPage.vue`): Pickup slot badges (`grey-3`) have clear perimeter definitions.
     - Admin rounds management (`RoundsPage.vue`, `RoundManagementModal.vue`): Round fruit badges are crisp and legible.
2. **Verification**:
   - `pnpm run typecheck` (`vue-tsc --noEmit`): PASSED (0 errors).
   - `pnpm run build` (`quasar build` SPA): PASSED (0 errors).

---

## 2. What Was Completed in Session (v0.7.4)
1. **Removed Payment Method Choice & Enforced Pay-at-Car Model**:
   - In `src/pages/IndexPage.vue`, completely removed `PaymentMethodPicker` and the advance payment option (`PROMPTPAY_PREPAID`).
   - Replaced with a reassuring customer banner:
     `💵 ชำระเงินตอนรับของที่ท้ายรถ (เงินสด / สแกน QR)` — *"ไม่ต้องโอนเงินล่วงหน้า ตรวจรับผลไม้แล้วค่อยจ่ายเงินสด หรือสแกน QR พร้อมเพย์กับคนขายที่รถ"*.
   - Locked order creation payload strictly to `paymentMethod: 'PAY_AT_CAR'` and `paymentStatus: 'UNPAID'`.
   - Reclaimed screen space and eliminated checkout friction and pre-payment discrepancies for variable weight fruits (Durian).
   - Removed unused `PaymentMethodPicker` import, which tree-shook `promptpay-qr` out of the customer storefront bundle (reducing chunk size from 33.27 KB to 28.68 KB).
2. **Verification**:
   - `pnpm run typecheck` (`vue-tsc --noEmit`): PASSED (0 errors).
   - `pnpm quasar build -m pwa`: PASSED (0 errors).

---

## 2. What Was Completed in Session (v0.7.3)
1. **Root Cause Analysis & Fix for Price Discrepancy (105 vs 100 THB)**:
   - **Root Cause**:
     - In `OrderDetailPage.vue` line 156: calculated fixed fruit price as `(item.orderedKg || 1) * item.pricePerKg` (e.g. `3 * 35 = 105 บาท`), completely ignoring the bundle discount (`ชุด 3 กก. (100 บาท)`).
     - In `fruitStore.ts` (line 287) and `OrderDetailPage.vue` (line 560): when durian was weighed and saved, grand total recalculation iterated items using `(it.orderedKg || 1) * it.pricePerKg`, losing the bundle discount.
     - In `FruitSelector.vue`: when customer selected a bundle, `itemFinalPrice` was not set on the emitted `OrderItem`.
   - **Fix**:
     - Built centralized pricing engine `src/utils/pricing.ts` (`calculateItemSubtotal` and `calculateOrderFinalTotal`) that checks `item.itemFinalPrice`, parses `item.orderedBundle` (regex `/\d+\s*บาท/`), matches product catalog bundles, or falls back to `orderedKg * pricePerKg`.
     - In `FruitSelector.vue`, assigned `itemFinalPrice: matchingBundle ? matchingBundle.price : qty * prod.pricePerKg`.
     - In `fruitStore.ts` (`updateWeighedFruit`), `OrderDetailPage.vue` (`handleSaveWeight`, item display, `currentFinalPrice`), `AnalyticsPage.vue` (`fruitStats`, `orderAnalysisList`), and `IndexPage.vue` (`totalEstimatedPrice`), unified all subtotal calculations under `calculateItemSubtotal`.
2. **Durian Scale Combobox Selector with Strict Numeric Typing**:
   - Converted the inline weight input in `OrderDetailPage.vue` into an interactive combobox selector `<q-select>`:
     - `use-input`, `fill-input`, `hide-selected`, `behavior="menu"`, `options-dense`.
     - 30 predefined weight options (`1.5` - `4.0` in 0.1 kg increments + `4.2`, `4.5`, `5.0`).
     - Real-time search filter matching typed query against options.
     - Strict typing validation: `inputmode="decimal"`, `@keydown` blocking non-numeric keys, `@input-value` regex filtering (`[^0-9.]`) allowing only digits and at most one decimal point.
     - Enter key shortcut (`@keyup.enter="handleSaveWeight(idx)"`) and retained 6 quick preset chips (`2.0`, `2.4`, `2.6`, `2.8`, `3.0`, `3.2` kg).
3. **Verification**:
   - `pnpm run typecheck` (`vue-tsc --noEmit`): PASSED (0 errors).
   - `pnpm quasar build -m pwa`: PASSED (0 errors).

---

## 2. What Was Completed in Session (v0.7.2)
1. **Removed Redundant Static Scan QR Banner in DispatchPage**:
   - In `src/pages/admin/DispatchPage.vue`, completely removed `<!-- Quick Action: Scan Customer QR Code -->`.
   - The Floating Action Button (FAB) `<q-page-sticky position="bottom-right" :offset="[16, 76]">` now serves as the single, non-redundant scan button accessible from any scroll position, reclaiming 60px of vertical viewport.
2. **Enforced Workspace Rule 8 (Automatic Version Bump & Git Commit Discipline)**:
   - Formally codified Rule 8 in `AGENTS.md` and `GEMINI.md`:
     - Every completed task/fix MUST bump version in `package.json` and `.agent_handoff.md`.
     - Every completed task/fix MUST perform `git add .` and Git commit with a concise, descriptive message.
3. **Verification**:
   - `pnpm run typecheck` (`vue-tsc --noEmit`): PASSED (0 errors).

---

## 2. What Was Completed in Session (v0.7.1)
1. **Floating Action Button (FAB) for QR Scan on Dispatch Desk**:
   - In `src/pages/admin/DispatchPage.vue`, added a prominent Floating Action Button `<q-page-sticky position="bottom-right" :offset="[16, 76]">` with `<q-btn fab color="positive" icon="qr_code_scanner" label="สแกน QR">`.
   - Allows seller to trigger camera QR scanner at any scroll position when inspecting orders at the tailgate.
2. **Deep Root Cause Fix: Unsupported Field Value `undefined` in Firestore**:
   - **Diagnosis**: Firestore `setDoc()` and `updateDoc()` crash with `FirebaseError: Function ... called with invalid data. Unsupported field value: undefined`.
     - In `createRound` & `updateRound`: Products like `NGO` evaluated `sizeTiers: undefined`, and other fruits evaluated `bundles: undefined`.
     - In `revertOrder` & `updateOrderStatus`: Reverting order status passed `completedAt: undefined`.
   - **Structural Zero-Regression Architecture**:
     - Built `sanitizeFirestoreData<T>()` in `src/stores/fruitStore.ts` that recursively cleans all `undefined` keys before any `setDoc` or `updateDoc` write.
     - In `createRound` and `updateRound`, constructed `prodItem` with optional conditional spreading `...(bundles ? { bundles } : {})` and `...(sizeTiers ? { sizeTiers } : {})`.
     - In `updateOrderStatus`, automatically converts any key set to `undefined` into Firestore's native `deleteField()` so fields are cleanly removed in Firestore without crashing, while also deleting the key from local memory state.
3. **Forensic Analysis of `startTime` Error**:
   - Traced `VM3429:2 Uncaught TypeError: Cannot read properties of undefined (reading 'startTime') at et.reportAllChanges`.
   - Verified that `.startTime` does not exist in project source code; it originates from an external browser extension (Web Vitals / Performance DevTools profiler).
4. **Verification**:
   - `pnpm run typecheck` (`vue-tsc --noEmit`): PASSED (0 errors).
   - `pnpm quasar build -m pwa`: PASSED (0 errors).

---

## 2. What Was Completed in Session (v0.7.0)
1. **Eliminated Duplicate QR Scanner Buttons**:
   - In `src/pages/admin/DispatchPage.vue`, removed the redundant bottom floating action button (FAB). Retained single prominent top scan button `[ 📷 สแกน QR Code ลูกค้าเพื่อเปิดออเดอร์ & ส่งมอบ ]`.
2. **Refactored Customer Phone Call Button in Order Detail Desk**:
   - In `src/pages/admin/OrderDetailPage.vue`, replaced the awkward wrapped block button with a native, circular green action button (`round color="positive" icon="phone" size="md"`) neatly right-aligned in a non-wrapping `<q-item>` structure.
   - Sanitized duplicate floor string (`order.customer.floor.replace(/^ชั้น\s*/, '')`), eliminating "ชั้น ชั้น G".
3. **Removed Cluttered Date Shortcut Chips in Round Edit**:
   - In `src/pages/admin/RoundEditPage.vue`, removed date preset chips ("วันนี้", "พรุ่งนี้", "วันอังคารหน้า", "วันศุกร์หน้า"), leaving only the clean calendar picker input.
4. **Single Standby Time Window for Seller (Admin Side)**:
   - In `src/pages/admin/RoundEditPage.vue`, replaced discrete pickup slot chips with a single **Standby Time Window** when the seller waits at the vehicle (e.g. `19:00 - 23:00` น.).
   - Added `standbyStartTime`, `standbyEndTime`, and live preview banner. Persisted to Firestore (`standbyTime`, `standbyStartTime`, `standbyEndTime`, `pickupSlots = [standbyTime]`).
   - Updated `RoundsPage.vue` and `BatchHeaderCard.vue` to display the dynamic standby window.
5. **Customer Approximate Arrival Time with Strict Standby Gating**:
   - In `src/components/customer/PickupSlotPicker.vue`, clearly displays the seller's standby window (`คนขายจะ Standby รอส่งของที่รถ: 19:00 - 23:00 น.`).
   - Customer enters/picks an **approximate arrival time (e.g. 19:30 น., single point in time, not an interval)** with quick 30-min suggestion chips or exact 24h clock picker.
   - Live boundary validation: immediately displays animated red warning alert banner if the customer's selected time falls outside the seller's standby window.
   - In `src/pages/IndexPage.vue`, gates the order submission button: customer cannot place an order if arrival time is outside the standby window.
6. **Cleaned Up Cluttered Admin Header**:
   - In `src/layouts/AdminLayout.vue`, removed oversized green avatar and redundant "แอดมิน" subtext.
   - Neatly arranged brand mascot logo, "Fruit Drop" title, user display name, compact role badge, and logout action within mobile viewport budget.
7. **Verification**:
   - `pnpm run typecheck` (`vue-tsc --noEmit`): PASSED (0 errors).
   - `pnpm quasar build -m pwa`: PASSED (0 errors).

---

## 2. What Was Completed in Session (v0.6.0)
1. **Header Layout Clutter Resolution**:
   - Removed the overlapping "หน้าร้าน" button from `AdminLayout.vue`.
   - Converted the Brand Logo Mascot and Title into a 1-tap navigation link to customer storefront (`to="/"`) with touch feedback and tooltip.
2. **Prevented Accidental Handover (Human Error Protection)**:
   - Removed all mutating inline buttons from `TailgateOrderCard.vue`.
   - Tapping an order card now cleanly navigates to the dedicated full-page **Order Detail** (`/admin/orders/:orderId`).
3. **Full-Page Order Detail Desk (`OrderDetailPage.vue`)**:
   - Customer profile details with 1-tap `tel:` dialer.
   - Durian digital scale calculator with quick preset chips (2.0 - 3.2 kg) and instant recalculation.
   - Proof photo attachment with camera capture (`capture="environment"`) and file picker.
   - Clear semantic handover buttons (`[ 💵 รับเงินสด X บ. & ส่งมอบ ]` and `[ ✓ ลูกค้าโอนแล้ว & ส่งมอบ ]`) with guardrails preventing delivery if durian is unweighed.
4. **Dynamic PromptPay Payment QR (Customer Scans Seller Phone)**:
   - Automatically generates official Thai PromptPay QR (`0878902935`, นาตยา บุญณะ) with exact amount.
   - If order contains unweighed durian, gates the QR behind an amber alert until weight is entered; once saved, QR generates immediately on screen.
5. **Customer Personal Order QR Code**:
   - In `OrderQueueCard.vue`, renders a high-contrast QR code encoding the direct order URL so customers can present it at the tailgate.
   - Includes download button for saving to photo gallery.
6. **Admin In-App Camera QR Scanner (`QrScannerPage.vue`)**:
   - Created `/admin/scan` using `html5-qrcode` with targeting brackets, torch toggle, camera switcher, and manual input fallback.
   - Instantly opens `/admin/orders/:orderId` upon detection.
7. **Verification**:
   - `pnpm run typecheck` (`vue-tsc --noEmit`): PASSED (0 errors).
   - `pnpm quasar build -m pwa`: PASSED (0 errors).

---

## 2. What Was Completed in Session (v0.5.1)
1. **Light Theme Conversion Across Admin (Zero Eye Strain)**:
   - Converted `AdminLayout.vue` and all admin pages (`DispatchPage.vue`, `RoundsPage.vue`, `RoundEditPage.vue`, `AnalyticsPage.vue`, `HarvestPage.vue`, `UsersPage.vue`) from dark mode to unified Quasar Light tokens.
   - Clean `bg-grey-1` background, pure white cards (`bg-white shadow-1`), high-contrast dark text (`text-grey-9`), and primary green accents matching customer storefront.
2. **Fixed Bottom Navigation Bar (AppSheet-Style)**:
   - Replaced top navigation with fixed bottom `<q-footer elevated class="bg-white text-grey-8 shadow-up-2">` using `<q-tabs>` and `<q-route-tab>`.
   - Navigation options: 🚗 ท้ายรถ (`/admin`), 📅 รอบส่ง (`/admin/rounds`), 📈 กำไร-ขาดทุน (`/admin/analytics`), 📦 ยอดเก็บ (`/admin/harvest`), 👥 ทีมงาน (`/admin/users`).
   - Added `padding-bottom: 76px` to 84px across all admin pages to prevent content clipping behind the fixed bottom bar.
3. **Storefront Direct Shortcut Button**:
   - Added prominent `[ 🛒 หน้าร้าน ]` button in the admin toolbar (`AdminLayout.vue`) with `to="/"` and `data-audit-id="btn-nav-to-customer-storefront"`.
   - Allows instant return from Admin to the customer ordering screen without manual URL tampering.
4. **Interactive Date Picker & Time Slot Selectors (Eliminated Manual Text)**:
   - **Pickup Date**: Replaced manual text input with an interactive Thai Date Picker popup (`<q-popup-proxy>`, `<q-date>`) plus 4 dynamic 1-click presets ("วันนี้", "พรุ่งนี้", "วันอังคารหน้า", "วันศุกร์หน้า"). Automatically formats into formal Thai Buddhist Era format (e.g. `วันอังคารที่ 8 กันยายน 2569`).
   - **Pickup Time Slots**: Replaced comma-separated text input with interactive toggleable chips/checkboxes with 8 standard presets (`17:00-18:00`, `18:00-18:30`, `18:30-19:00`, `19:00-19:30`, `19:30-20:00`, `20:00-20:30`, `20:30-21:00`, `21:00+`), plus custom slot adder with instant addition/deletion.
5. **Full Type Safety & Production PWA Build Verified**:
   - Resolved TypeScript `noUncheckedIndexedAccess` errors in `formatThaiDateFromIso()`.
   - Verified `pnpm run typecheck` and `pnpm quasar build -m pwa` (0 errors).

---

## 2. What Was Completed in Session (v0.5.0)
1. **Full-Page Architectural Overhaul (Eliminated Modals)**:
   - Completely eliminated the cramped header chips and popup modals in admin as requested by user.
   - Built a dedicated parent layout `src/layouts/AdminLayout.vue` with auth gate, role indicators, and sleek sub-navigation tabs.
   - Every admin section is now a dedicated full-page route with prominent `[ ‹ ย้อนกลับ ]` navigation buttons and native Android hardware/gesture back support (`router.back()`).

2. **Full-Page Admin Sub-Routes & Views**:
   - `/admin` (`DispatchPage.vue`): Streamlined Tailgate Dispatch Desk with real-time stats and durian scale calculator.
   - `/admin/rounds` (`RoundsPage.vue`): Full-page rounds list with status toggle switch and direct edit buttons.
   - `/admin/rounds/new` & `/admin/rounds/:roundId/edit` (`RoundEditPage.vue`): Comprehensive create & edit form for existing preorder rounds. Configures title, dates, pickup location, pickup slots, KBANK / PromptPay credentials, status toggle, and all 7 fruits with selling prices, `costPerKg`, and quotas.
   - `/admin/analytics` (`AnalyticsPage.vue`): Deep Profit & Loss Performance Analysis with 4 KPI cards (Total Revenue, Fruit Cost / COGS, Gross Profit, Margin %), filters (Round, Status, Payment Method), fruit-level profit breakdown, and expandable order-level analysis.
   - `/admin/harvest` (`HarvestPage.vue`): Full-page orchard harvest logistics sheet with 1-click LINE message copy.
   - `/admin/users` (`UsersPage.vue`): Full-page 3-tier staff RBAC management (System Admin, Shop Owner, Seller).

3. **Profit & Loss Engine and Firestore Data Synchronization**:
   - Extended types and data models with `costPerKg` and `RoundUpdatePayload`.
   - Added `updateRound()`, `getRoundById()`, `getProductsByRoundId()` in `fruitStore.ts`.
   - Updated `scripts/seed_live_data.js` with `costPerKg` and seeded 4 realistic sample orders for live analytics and dispatch testing.
   - Successfully verified with `pnpm run typecheck` and `pnpm quasar build -m pwa` (0 errors).

---

## 2. Previous Accomplishments (v0.4.1)
1. **Removed Redundant Mascot Logo from `BatchHeaderCard.vue`**:
   - Eliminated `<!-- Mascot Logo -->` and `q-img` from the round details header to reclaim mobile vertical screen space.

2. **Durian Tier Label Cleanup**:
   - Stripped `★ ยอดนิยม` from the medium durian tier across codebase (`fruitStore.ts`, `RoundManagementModal.vue`) and Firestore documents, leaving clean `ลูกกลาง (2.1 - 3.0 กก.)`.

3. **Purged Hardcoding & Seeded Authentic Live Firestore Master Data**:
   - Replaced static client fallbacks with dynamic Firestore document lookups in `userStore.ts` and `fruitStore.ts`.
   - Executed `scripts/seed_live_data.js` to seed genuine Firestore records:
     - `users/natyabuyna089@gmail.com` (นาตยา บุญณะ, 0878902935, SHOP_OWNER)
     - `users/wittinunt.k@gmail.com` (Wittinunt Khansuwan, 0653539941, SYSTEM_ADMIN)
     - `rounds/ROUND-001` (Active round with verified dates, slots, and payment info)
     - `products/` (PROD-ROUND-001-NGO, PROD-ROUND-001-THURIAN, PROD-ROUND-001-MANGKUT, PROD-ROUND-001-LONGKONG) with authentic typed maps/arrays.

4. **Integrated Real KBANK & PromptPay Account Credentials**:
   - Added `bankName: 'KBANK (กสิกรไทย)'`, `bankAccountNumber: '8172235408'`, `promptPayNumber: '0878902935'`, and account owner `'นาตยา บุญณะ'` to `PreorderRound`, `RoundManagementModal.vue`, `PaymentMethodPicker.vue`, and Firestore.
   - Customers can now either scan PromptPay QR or transfer directly to the KBANK account.

---

## 2. Previous Accomplishments (v0.3.2)
1. **Firebase Authentication Credentials & API Key Root Cause Fix**:
   - Discovered and resolved `API key not valid` (HTTP 400 Bad Request) on Google Identity Toolkit (`identitytoolkit.googleapis.com`).
   - Fetched authentic SDK credentials directly from Google Cloud Firebase via `firebase apps:sdkconfig WEB 1:660120514549:web:d5eae93b24dc81392fbabc --project fruit-drop-aon`.
   - Updated `src/boot/firebase.ts` with authentic API key (`AIzaSyBOuMmeEjp3uyTLRzox8q2SHONS3agbQog`), exact project number (`660120514549`), and valid appId, unblocking Google Auth login.

2. **Analysis of Extension vs System Errors**:
   - Diagnosed `webextension.js:26 Uncaught TypeError`: Identified as injected content scripts from browser extensions (e.g., password managers, adblockers), completely external to the application codebase.

---

## 2. Previous Accomplishments (v0.3.1)
1. **Redundant Header Clutter Elimination (ActiveRoundsList.vue)**:
   - Removed duplicate mascot avatar, "Fruit Drop", and subtitle block from the top of the customer round selection screen.
   - Preserves vertical mobile screen space so active rounds are immediately visible at the top.

2. **Firestore Composite Index Elimination & Zero-Regression Query Optimization**:
   - Refactored `subscribeToOpenRounds` and `subscribeToOrders` in `src/stores/fruitStore.ts`.
   - Removed database-level `orderBy('createdAt', 'desc')` in composite queries that caused `FirebaseError: The query requires an index`.
   - Implemented type-safe numerical sorting in client memory (`(Number(b.createdAt) || 0) - (Number(a.createdAt) || 0)`), eradicating console warnings.

---

## 2. Previous Accomplishments (v0.3.0)
1. **Total Brand Purge of "สวนบ้านเรา" -> Pure "Fruit Drop"**:
   - Eliminated all occurrences of "สวนบ้านเรา" across UI templates, dialogs, banners, metadata, PWA manifests, and documentation:
     - `index.html` title: **Fruit Drop - สั่งจองผลไม้สดจากสวน**
     - `src-pwa/manifest.json`: Name and short_name set to **Fruit Drop**
     - `package.json`: Product name and description updated
     - `AdminHeaderBar.vue`: Removed `"สวนบ้านเรา | แอดมินคุณอ้น & ครอบครัว"` -> Replaced with clean **Fruit Drop • แผงจัดการท้ายรถ**
     - Purged from customer components: `BatchHeaderCard.vue`, `ContactPicker.vue`, `PickupSlotPicker.vue`, `PaymentMethodPicker.vue`
     - Verified via `grep_search`: **0 matches** of "สวนบ้านเรา" in `src/`.

2. **Root Cause Fix for Invisible Customer Order Button**:
   - **Root Cause**: Quasar's `<q-footer>` in `src/layouts/MainLayout.vue` occupied a fixed position (`z-index: 2000`) at the bottom of the viewport, permanently overlapping and occluding the sticky order button on mobile screens.
   - **Fix**: Removed persistent `<q-footer>` from `MainLayout.vue`.
   - In `src/pages/IndexPage.vue`, added a high-contrast, prominent inline order confirmation block:
     `[ ยืนยันการสั่งจอง (xxx บาท) ]` directly following the pickup and payment selections, backed up by a floating bottom bar during scroll.

3. **Admin Authentication Gate (`/admin`)**:
   - Direct access to `/admin` without authentication is completely blocked.
   - `src/pages/AdminPage.vue`:
     - **Unauthenticated**: Displays clean Dark Slate Login Card with Google Sign-In button (`fruitStore.signInWithGoogle()`).
     - **Authenticated but Non-Staff**: Displays Access Denied card (`ไม่มีสิทธิ์เข้าถึงระบบผู้ดูแล`) with Logout button.
     - **Authenticated Staff (`SYSTEM_ADMIN`, `SHOP_OWNER`, `SELLER`)**: Grants access to the full Tailgate Dispatch Desk.

4. **Admin Pre-order Round Creator & Management**:
   - Built `src/components/admin/RoundManagementModal.vue`:
     - **Tab 1 (รอบทั้งหมด)**: View active/closed rounds, instant toggle switch (`isOpen`), and button to set the active round for dispatch.
     - **Tab 2 (เปิดรอบใหม่)**: Create round specifying title, pickup date, location, pickup time slots, PromptPay account info, and fruit checklist (เงาะ, ทุเรียน, มังคุด, ลองกอง, ลางสาด, ส้ม, มะม่วง) with custom price/kg and available quota (kg).
   - In `src/stores/fruitStore.ts`: Added `createRound()`, `toggleRoundStatus()`, `selectActiveRound()`, and atomic Firestore creation of `rounds/{roundId}` and `products/PROD-{roundId}-{fruit}`.
   - Added `[ ➕ จัดการรอบจอง ]` action button directly in `AdminHeaderBar.vue`.

5. **Customer 2-Stage Flow (Clean, Crisp, Human Thai Copy)**:
   - **Stage 1 (เลือกรอบส่ง)**: When visiting `http://localhost:9000/`, customer sees `ActiveRoundsList.vue` displaying only open rounds (`isOpen == true`). Past/draft rounds are hidden.
   - **Stage 2 (เลือกผลไม้และสั่งจอง)**: Clicking `[ สั่งจองรอบนี้ ➔ ]` navigates into the round's fruit selector and booking form. Features prominent top and bottom `[ ‹ ย้อนกลับไปเลือกรอบ ]` buttons.
   - Purged all verbose AI filler text ("คัดเกรดพรีเมียม สดใหม่ส่งตรงจากสวน...", "กรอกเพียงครั้งเดียว ระบบจะจำข้อมูลนี้ไว้ให้อัตโนมัติ...") in favor of crisp, direct human wording.

6. **Firestore Data Seed**:
   - Seeded active round: `rounds/ROUND-001` (`title: "รอบส่งผลไม้ วันอังคาร 8 ก.ย."`, `isOpen: true`).
   - Seeded initial round products: `products/PROD-ROUND-001-NGO` (เงาะโรงเรียน 45 บ./กก.) and `products/PROD-ROUND-001-THURIAN` (ทุเรียนหมอนทอง 180 บ./กก.).

7. **Validation & Verification**:
   - `pnpm run typecheck` (`vue-tsc --noEmit`): **0 errors**.
   - `pnpm quasar build -m pwa`: **Build succeeded** (0 errors).

---

## 3. Current Project State & Active Context (v0.9.2)
- **Workspace**: `d:\GitHub\PWA\fruit-drop`
- **Current Version**: `0.9.2`
- **Build Output**: `dist/spa/` and `dist/pwa/` fully compiled and verified (`pnpm run typecheck` & `pnpm run build` passing with 0 errors).
- **Local Dev Server**: Running on `http://localhost:9000/`.
- **Firebase Project**: `fruit-drop-aon` (Region: `asia-southeast1`).
- **Users**:
  - `wittinunt.k@gmail.com` -> `SYSTEM_ADMIN`
  - `natyabuyna089@gmail.com` -> `SHOP_OWNER`
- **Active Firestore Collections**: `users`, `rounds`, `products`, `orders`, `master_fruits`.
- **Storage Buckets**: `fruits/` (master fruit photos), `orders/` (payment slips & cash handover proofs).
- **Core Architecture Highlights**:
  - **Master Fruit Catalog CRUD**: Fully dynamic fruit management in `/admin/fruits` and in-place modal creation in `/admin/rounds/new`.
  - **Snapshot Isolation**: Round-specific products copy data from master catalog; historic orders snapshot prices, eliminating cross-round mutations.
  - **Clean M3 Expressive Tokens**: 16px surface cards, 10px inner containers, 9999px pill buttons/badges.
  - **Ergonomic Safety**: Separated deck card actions (toggle far left, edit far right) preventing fat-finger misclicks.
  - **Header Purity**: Uncluttered top bar without small redundant icon buttons.

---

## 4. Pending Tasks / Next Steps for Next Session
1. **Production Deployment**:
   - Run `firebase deploy --only hosting,firestore:rules,storage` to push v0.9.2 live.
2. **End-to-End Field Validation**:
   - Test creating a brand-new custom fruit with uploaded camera photo in `/admin/fruits`.
   - Test creating a new round including that custom fruit and placing an order from the customer storefront (`/`).
   - Test tailgate fulfillment and weighing scale flow on the active order.
